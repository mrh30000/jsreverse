# 【JS逆向】某手机厂商登录逆向分析

> **作者**: littlewhite11 | **发布时间**: 2024-12-14 17:23:00 | **版块**: 『脱壳破解区』 | **查看/回复**: 4993 / 41
> **原文**: [https://www.52pojie.cn/thread-1991268-1-1.html](https://www.52pojie.cn/thread-1991268-1-1.html)

---

*本帖最后由 littlewhite11 于 2025-3-10 07:39 编辑*

## 逆向目标

* 网址：`aHR0cHM6Ly9wYXNzcG9ydC52aXZvLmNvbS5jbi8jL2xvZ2lu`
* 目标：表单数据加密，响应数据解密

## 抓包分析

我们选择密码登录，输入手机号和密码登录，会发起一个`login`请求，其中表单数据`encData`和`encKey`是需要逆向的。

![](https://attach.52pojie.cn/forum/202412/14/172136o6774k88f599agja.png)

响应数据是加密的，也需要进行逆向分析。

![](https://attach.52pojie.cn/forum/202412/14/172138uw4hv3sg770w0sim.png)

## 逆向分析

我们全局搜索`encData`，最终搜索到下图的地方，直接下断，重新请求，成功断住，`encKey`也在此处。

`e.params`中的`nounce`和`pwd`也需要进行分析，但我们先分析`encData`和`encKey`的生成。

![](https://attach.52pojie.cn/forum/202412/14/172140z1n3119yql78ydqs.png)

首先是`y`函数，将`e.params`处理成一个字符串。

![](https://attach.52pojie.cn/forum/202412/14/172142vm3aww8ndbnd81sr.png)

然后是`encrypt`函数，对`ae().enc.Utf8.parse(t)`这种写法比较熟悉的大佬都知道应该是用了`crypto-js`这个加密库。

![](https://attach.52pojie.cn/forum/202412/14/172143flpxi9j8chhxxppd.png)

可以用`crypto-js`这个库复现加密，经验证，是标准的AES，一个随机key，一个固定iv。

```
const CryptoJS = require('crypto-js')
function encrypt(e){
    var t = function() {
    for (var e = "", t = "0123456789abcdef", n = 0; n < 16; n++)
            e += t.charAt(Math.floor(16 * Math.random()));
        return e
    }()
    , n = CryptoJS.enc.Utf8.parse(t)
    , a = CryptoJS.enc.Utf8.parse("16-Bytes--String");
    var r = CryptoJS.enc.Utf8.parse(e);
    i = CryptoJS.AES.encrypt(r, n, {
        iv: a,
        mode: CryptoJS.mode.CBC,
        padding: CryptoJS.pad.Pkcs7
    })
    return i.ciphertext.toString()
}
```

而我们要的`encKey`也在此处`enckey: ee(t)`，这个跟进去会发现就是一个RSA加密，可以用`node-jsencrypt`这个标准库解决。

![](https://attach.52pojie.cn/forum/202412/14/172145lwwnff80ww77qzun.png)

```
const JSEncrpyt = require('node-jsencrypt')
function rsa_encrypt(e){
    let rsa = new JSEncrpyt()
    rsa.setPublicKey("公钥")
    return rsa.encrypt(e)
}
```

那有人就会好奇，`o.Z.commit("setEncrypt", {enckey: ee(t),aeskey: n,aesiv: a})`这一步在干什么，先卖个关子，后文会分析。

该案例用了一种常见的安全设计模式，结合了AES和RSA两种加密算法的优点，将两者结合使用既保证了安全性，又能有较高的效率。

> AES在对大量数据进行加解密的时候速度快
>
> RSA的安全性基于大数分解的数学难题，破解难度极高

AES作为对称加密的一种，密钥安全性尤为重要，且密钥是前后端共享的，如果密钥在传输过程中被截获，数据的安全性将无法保证。

而RSA加密速度较慢，适合加密较小的数据（如密钥），可以用于加密AES的密钥，确保密钥在传输过程中不会被窃取。

表单参数的加密搞定了，还需要看`e.params`中的`pwd`和`nonce`是怎么来的。

首先看`nounce`，就在`encData`上面一点的地方，`nounce`由一个时间戳和一个随机数生成，而`m()`的返回值大概率是个hash函数，其实`nounce`就是一个随机值。

![](https://attach.52pojie.cn/forum/202412/14/172147yo2n7sn2r1rz2r7o.png)

遇到hash函数，我们可以尝试对字符串`'1'`进行hash看是不是标准算法，`64`的长度，`6b8b`开头，应该就是标准的`sha256`。

![](https://attach.52pojie.cn/forum/202412/14/172149ute8knktvxe9949x.png)

记住一些常见hash函数的特征，有时候就可以在控制台验证它是不是标准的算法，能够稍微提高那么一点效率。

然后是`pwd`，往前看，它已经生成了，并且如果往前跟栈就到异步之前了，那没办法，我们就在`s.request`处下断，重新请求。

![](https://attach.52pojie.cn/forum/202412/14/172151r5ao0z25xxcoov8l.png)

可以看到，`pwd`已经生成了，也就是说`pwd`是在异步之前就已经生成了，那我们就没必要跟异步了，这一点很重要，做逆向一定要细心，不要觉得密文往前跟就是异步了，就认为一定是异步生成的。

![](https://attach.52pojie.cn/forum/202412/14/172154snxnnn3mx0o3ouox.png)

我们继续往前跟栈，发现`pwd`是在这生成的。

![](https://attach.52pojie.cn/forum/202412/14/172156gg25m5w4n4c45mug.png)

我们点进去看看，发现还是一个RSA，只不过是公钥换了。

![](https://attach.52pojie.cn/forum/202412/14/172158kqv2qnchxndax690.png)

至此我们的表单参数就搞定了，下面继续看响应数据加密。

老规矩还是`hook JSON.parse`，成功断住。

![](https://attach.52pojie.cn/forum/202412/14/172200r44x7ozfa1yi4r74.png)

往上跟一个栈，就会找到解密的位置。`encKey`熟不熟悉，没错，其实就是表单的`encKey`。

![](https://attach.52pojie.cn/forum/202412/14/172203o5595xnzefw61zwu.png)

我们进去`decrypt`函数看看，发现是AES解密，那这就信手拈来了，导包解决即可。

![](https://attach.52pojie.cn/forum/202412/14/172205azo9ciltq872lojm.png)

```
const CryptoJS = require('crypto-js')
function decrypt(e, t){
    var n = CryptoJS.enc.Hex.parse(e)
      , a = CryptoJS.enc.Base64.stringify(n);
    var aeskey = CryptoJS.enc.Utf8.parse('之前的key');
    var aesiv = CryptoJS.enc.Utf8.parse("16-Bytes--String")
    var r = CryptoJS.AES.decrypt(a, aeskey, {
        iv: aesiv,
        mode: CryptoJS.mode.CBC,
        padding: CryptoJS.pad.Pkcs7
    });
    return r.toString(CryptoJS.enc.Utf8).toString()
}
```

前文一些操作的谜底也终于揭开了，`o.Z.commit("setEncrypt", {enckey: ee(t),aeskey: n,aesiv: a})`其实就是将`key`和`iv`保存起来，然后解密的时候也用同样的`key`和`iv`。

好了，加密和解密都解决了，直接模拟请求。

![](https://attach.52pojie.cn/forum/202412/14/172207fx28nnza2png8dxn.png)

成功！！！

> 网站多请求几次会触发某象的验证码，以后有机会再分析怎么解决。
