# c#,.net Selenium 反反爬配置

> **作者**: bjszz | **发布时间**: 2022-12-25 10:57:00 | **版块**: 『编程语言区』 | **查看/回复**: 5504 / 7
> **原文**: [https://www.52pojie.cn/thread-1730071-1-1.html](https://www.52pojie.cn/thread-1730071-1-1.html)

---

最近用c#写自动登录淘宝的代码，由于没有配置反反爬，淘宝一直跳出滑块验证，大家知道淘宝的滑块就像极验的滑块，Selenium 用图鉴然后调用链也是是过不去的的。
但是我配置了Selenium，滑块就不在出现了，直接输入密码和账号就自动登录了，配置的代码如下。
配置的第一块，可能老的IWebDriver 需要配置这里。

[C#] *纯文本查看*

```

 IWebDriver webDriver = null;
                            if (webDriver == null)
                            {
                                webDriver = function.getdriver(false, false);
                                IJavaScriptExecutor js = (IJavaScriptExecutor)webDriver;
                                string returnjs = (string)js.ExecuteScript("Object.defineProperties(navigator, {webdriver:{get:()=>undefined}});");
                            }
```

第二个配置的地方。

[C#] *纯文本查看*

```

options.AddArgument("--disable-gpu");
            options.AddExcludedArgument("enable-automation");
            options.AddArgument("--disable-blink-features=AutomationControlled");
```

欢迎交流
