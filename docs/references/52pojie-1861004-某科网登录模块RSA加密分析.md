# 某科网登录模块RSA加密分析

> **作者**: laoxiaodiao | **发布时间**: 2023-11-23 22:51:00 | **版块**: 『编程语言区』 | **查看/回复**: 1030 / 19
> **原文**: [https://www.52pojie.cn/thread-1861004-1-1.html](https://www.52pojie.cn/thread-1861004-1-1.html)

---

*本帖最后由 laoxiaodiao 于 2023-11-23 22:54 编辑*

看到求助板块经常有道友经常求助下载某科网资料，故想分析分析登录模块
（以前也做过某科网自动登录下载的，但是网站升级，对登录板块进行了升级，后面也没时间再弄了）
先看登录界面源码

![](https://attach.52pojie.cn/forum/202311/23/221449qa87872eddee2zf3.png)

在源码中搜索accountLoginForm

![](https://attach.52pojie.cn/forum/202311/23/221903e4aff0no1r753orf.png)

发现有encryptPwd，继续搜索encryptPwd函数

![](https://attach.52pojie.cn/forum/202311/23/222015zd764hzb060b6hih.png)

发现主要是encryptE1函数进行加密，该函数是由getEncrypValue加密而来，继续搜索getEncryptValue，该函数还是在当前文件

![](https://attach.52pojie.cn/forum/202311/23/222301lalpj9u9ua88xugj.png)

注意：出现了Pubkey,分析该加密函数，应该是RSA加密，而且加密的前还通过js文件进行了处理，拼接了一些字符串进去得到newValue，和密码一起进行RSA加密。
后面实验发现，js的加密函数每次刷新都不一样。
所以模拟登录的方案大概出来了，
step1：请求当前页面，获取js加密函数以及RSA密钥
step2：模拟js函数拼接密码,得到newValue
step3：对newValue进行RSA加密
step4：提交后台，模拟登录

好，开始码代码，获取所需要的js函数以及RSA密钥

[Python] *纯文本查看*

```

import json
from RSA_jiami import encryption
import execjs,re
import requests
def getjs():
    cookie=open("cookie.txt","r").read()
    url="https://sso.zxxk.com/login"
    header={
    "Accept": "text/html,application/xhtml+xml,application/xml;q=0.9,image/webp,image/apng,*/*;q=0.8,application/signed-exchange;v=b3;q=0.7",
    "Accept-Encoding": "gzip, deflate, br",
    "Accept-Language": "zh-CN,zh;q=0.9,en;q=0.8,en-GB;q=0.7,en-US;q=0.6",
    "Cache-Control": "no-cache",
    "Connection": "keep-alive",
    "Host": "sso.zxxk.com",
    "Pragma": "no-cache",
    "Sec-Fetch-Dest": "document",
    "Sec-Fetch-Mode": "navigate",
    "Sec-Fetch-Site": "same-origin",
    "Sec-Fetch-User": "?1",
    "Upgrade-Insecure-Requests": "1",
    "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/118.0.0.0 Safari/537.36 Edg/118.0.2088.76",
    "sec-ch-ua": '''"Chromium";v="118", "Microsoft Edge";v="118", "Not=A?Brand";v="99"''',
    "sec-ch-ua-mobile": "?0",
    "sec-ch-ua-platform": "Windows",
    "Cookie":cookie
    }
    html=requests.get(url=url,headers=header).text
    # html_headers=requests.get(url=url,headers=header).headers
    js=re.findall('eval\("(.*?)"\);',html)[0]
    flowId=re.findall('<input type="hidden" name="flowId" value="(.*?)">',html)[0]
    execution=re.findall('<input type="hidden" name="execution" value="(.*?)"/>',html)[0]
    hanshu=re.findall("function (.*?)\(.*?\){",js)[0]
    canshu=re.findall("\((.*?)\){",js)[0]
    return flowId,execution,hanshu,canshu,js
```

这里的flowid，execution等都是后面要发给后端的参数，一并提取
接才来码第二步和第三步代码

[Python] *纯文本查看*

```

def jiami(hanshu,canshu,js,text):
    # flowId,execution,hanshu,canshu,js=getjs()
    ctx = execjs.compile(js)
    result = ctx.call(hanshu, text)
    print("新value："+result)  # 输出：
    publicKey="MIGfMA0GCSqGSIb3DQEBAQUAA4GNADCBiQKBgQC4o\/K\/FWvqfmqUrMn40tG4AocMdLcp5DWySXpPtDI6HM0k23aMGPkzmaxsyepHbHfNvAh\/76oGdp9kCpJEkfBx6t2fdNXnxAcu8SHynZvb+Vx56CtEZ9qX3NsH7rEOuFPiIl02jDcOgGdnSE4ViSsB5OYYOMk48STW08Zf2FB2TQIDAQAB"
    publicKey="-----BEGIN PUBLIC KEY-----\n"+publicKey+"\n-----END PUBLIC KEY-----"
    text_encrypted_base64 = encryption(result, publicKey)
    print('密文：', text_encrypted_base64)
    return text_encrypted_base64
```

RSA加密函数如下

[Python] *纯文本查看*

```

import base64
from Crypto.Cipher import PKCS1_v1_5
from Crypto import Random
from Crypto.PublicKey import RSA
# ------------------------加密------------------------
def encryption(text: str, public_key: bytes):
    # 字符串指定编码（转为bytes）
    text = text.encode('utf-8')
    # 构建公钥对象
    cipher_public = PKCS1_v1_5.new(RSA.importKey(public_key))
    # 加密（bytes）
    text_encrypted = cipher_public.encrypt(text)
    # base64编码，并转为字符串
    text_encrypted_base64 = base64.b64encode(text_encrypted).decode()
    return text_encrypted_base64

# ------------------------解密------------------------
def decryption(text_encrypted_base64: str, private_key: bytes):
    # 字符串指定编码（转为bytes）
    text_encrypted_base64 = text_encrypted_base64.encode('utf-8')
    # base64解码
    text_encrypted = base64.b64decode(text_encrypted_base64)
    # 构建私钥对象
    cipher_private = PKCS1_v1_5.new(RSA.importKey(private_key))
    # 解密（bytes）
    text_decrypted = cipher_private.decrypt(text_encrypted, Random.new().read)
    # 解码为字符串
    text_decrypted = text_decrypted.decode()
    return text_decrypted
```

接才来最后一步，提交登录

[Python] *纯文本查看*

```

def login(zh,mm):
    cookie = open("cookie.txt", "r").read()
    header = {
        "Accept": "text/html,application/xhtml+xml,application/xml;q=0.9,image/webp,image/apng,*/*;q=0.8,application/signed-exchange;v=b3;q=0.7",
        "Accept-Encoding": "gzip, deflate, br",
        "Accept-Language": "zh-CN,zh;q=0.9,en;q=0.8,en-GB;q=0.7,en-US;q=0.6",
        "Cache-Control": "no-cache",
        "Connection": "keep-alive",
        "Host": "sso.zxxk.com",
        "Pragma": "no-cache",
        "Sec-Fetch-Dest": "document",
        "Sec-Fetch-Mode": "navigate",
        "Sec-Fetch-Site": "same-origin",
        "Sec-Fetch-User": "?1",
        "Upgrade-Insecure-Requests": "1",
        "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/118.0.0.0 Safari/537.36 Edg/118.0.2088.76",
        "sec-ch-ua": '''"Chromium";v="118", "Microsoft Edge";v="118", "Not=A?Brand";v="99"''',
        "sec-ch-ua-mobile": "?0",
        "sec-ch-ua-platform": "Windows",
        "Cookie": cookie,
        "referer":"Referer: https://sso.xkw.com/login?service=https%3A%2F%2Faccount.xkw.com%2Fuc%2Frouter%3FserviceUrl%3Dhttps%25253A%252F%252Faccount.xkw.com%252F%252523%252Fsetting-index"
    }
    url="https://sso.xkw.com/login?service=https%3A%2F%2Faccount.xkw.com%2Fuc%2Frouter%3FserviceUrl%3Dhttps%25253A%252F%252Faccount.xkw.com%252F%252523%252Fsetting-index"
    flowId, execution, hanshu, canshu, js = getjs()
    password=jiami(hanshu, canshu, js,mm)
    data={"username":zh,"password":password,"_eventId":"submit","methodType":"2","rememberMe":"true","execution":execution,"flowId":flowId,"geetest_offline":""}
    res=requests.post(url=url,headers=header,data=json.dumps(data))
    res_header=res.headers
    res_text=res.text
    open('index.html','w',encoding="utf-8").write(res_text)
    print(res_text)
```

其实，提交之后，她还有一步就是要验证码，但是本贴只是一个学习记录贴，就不贴出来啦。
抛砖引玉，各位大佬可以继续写下去，哈哈哈
