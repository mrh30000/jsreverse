# 58租房爬取已破解字体加密但是gui方面还不完善

> **作者**: cdsgg | **发布时间**: 2020-12-08 10:49:00 | **版块**: 『编程语言区』 | **查看/回复**: 1200 / 29
> **原文**: [https://www.52pojie.cn/thread-1323885-1-1.html](https://www.52pojie.cn/thread-1323885-1-1.html)

---

*本帖最后由 cdsgg 于 2020-12-8 16:44 编辑*

[Python] *纯文本查看*

```
# -*- coding: utf-8 -*-

# Form implementation generated from reading ui file '58租房.ui'
#
# Created by: PyQt5 UI code generator 5.15.2
#
# WARNING: Any manual changes made to this file will be lost when pyuic5 is
# run again.  Do not edit this file unless you know what you are doing.
import base64

from PyQt5 import QtCore, QtGui, QtWidgets
from PyQt5.QtCore import QThread, pyqtSignal
from PyQt5.QtWidgets import QApplication, QMainWindow, QHeaderView, QTableWidgetItem, QMessageBox
import sys
import asyncio
import xlsxwriter
from fontTools.ttLib import TTFont, BytesIO
from pyppeteer import launch
import re
from bs4 import BeautifulSoup
import tkinter
import os

def get_page_show_ret(mystr, bs64_str):
        font = TTFont(BytesIO(base64.decodebytes(bs64_str.encode())))
        # 可用于58 等字体加密的反爬爬取
        c = font['cmap'].tables[0].ttFont.tables['cmap'].tables[0].cmap
        ret_list = []
        for char in mystr:
                decode_num = ord(char)
                if decode_num in c:
                        num = c[decode_num]
                        num = int(num[-2:]) - 1
                        ret_list.append(num)
                else:
                        ret_list.append(char)
        ret_str_show = ''
        for num in ret_list:
                ret_str_show += str(num)
        return ret_str_show

def screen_size():
        tk = tkinter.Tk()
        width = tk.winfo_screenwidth()
        height = tk.winfo_screenheight()
        tk.quit()
        return {'width': width, 'height': height}

# await asyncio.sleep(5)

# await browser.close()
# workbook.close()

class Ui_Form(object):
        def setupUi(self, Form):
                Form.setObjectName("Form")
                Form.resize(460, 300)
                self.City = QtWidgets.QLabel(Form)
                self.City.setGeometry(QtCore.QRect(100, 80, 131, 16))
                self.City.setObjectName("City")
                self.Hous = QtWidgets.QLabel(Form)
                self.Hous.setGeometry(QtCore.QRect(100, 130, 131, 16))
                self.Hous.setObjectName("Hous")
                self.enterCity = QtWidgets.QLineEdit(Form)
                self.enterCity.setGeometry(QtCore.QRect(230, 80, 113, 20))
                self.enterCity.setObjectName("enterCity")
                self.startCrawler = QtWidgets.QPushButton(Form)
                self.startCrawler.setGeometry(QtCore.QRect(180, 190, 75, 23))
                self.startCrawler.setObjectName("startCrawler")
                self.softwareTitles = QtWidgets.QLabel(Form)
                self.softwareTitles.setGeometry(QtCore.QRect(130, 20, 181, 31))
                self.softwareTitles.setAlignment(QtCore.Qt.AlignCenter)
                self.softwareTitles.setObjectName("softwareTitles")
                self.HousingTypes = QtWidgets.QComboBox(Form)
                self.HousingTypes.setGeometry(QtCore.QRect(230, 130, 111, 22))
                self.HousingTypes.setObjectName("HousingTypes")
                self.progressBar = QtWidgets.QProgressBar(Form)
                self.progressBar.setGeometry(QtCore.QRect(160, 230, 231, 23))
                self.progressBar.setProperty("value", 0)
                self.progressBar.setObjectName("progressBar")
                self.Hous_2 = QtWidgets.QLabel(Form)
                self.Hous_2.setGeometry(QtCore.QRect(70, 230, 131, 21))
                self.Hous_2.setObjectName("Hous_2")
                self.HousingTypes.addItems(typeList)
                self.startCrawler.clicked.connect(self.startspider)

                self.retranslateUi(Form)
                QtCore.QMetaObject.connectSlotsByName(Form)

        def retranslateUi(self, Form):
                _translate = QtCore.QCoreApplication.translate
                Form.setWindowTitle(_translate("Form", "58租房爬虫"))
                self.City.setText(_translate("Form", "输入你所在的城市："))
                self.Hous.setText(_translate("Form", "选择房源类型："))
                self.enterCity.setPlaceholderText(_translate("Form", "请输入城市"))
                self.startCrawler.setText(_translate("Form", "开始爬虫"))
                self.softwareTitles.setText(_translate("Form", "58租房爬虫"))
                self.HousingTypes.setPlaceholderText(_translate("Form", "请选择房源类型"))
                self.Hous_2.setText(_translate("Form", "当前进度："))

        def startspider(self):
                # tbui=Ui_TabWidget        # w2.setColumnCount(8)
                QMessageBox.information(w,'提示','已经开始爬取由于数量比较大可能有点慢 耐心等待\n界面可能出现假死状态\n因为进度条还没写完有些问题')
                TB.toGetData()
                w2.show()
                w.hide()

        # w2.setVerticalHeaderLabels(typeList)
        # w2.setROwCount(3)

        def toObtain(self):
                ace = self.enterCity.text()
                return ace

        def Untility(self):
                aae = self.HousingTypes.currentText()
                return aae

# asyncio.get_event_loop().run_until_complete(main())

class Ui_Form2(object):
        def setupUi(self, Form):
                Form.setObjectName("Form")
                Form.resize(1867, 918)
                self.tableWidget = QtWidgets.QTableWidget(Form)
                self.tableWidget.setGeometry(QtCore.QRect(0, 130, 1867, 792))
                self.tableWidget.setObjectName("tableWidget")
                self.tableWidget.setColumnCount(4)
                self.tableWidget.setRowCount(0)
                self.tableWidget.horizontalHeader().setSectionResizeMode(QHeaderView.Stretch)
                self.tableWidget.setHorizontalHeaderLabels(tblist)
                self.pushButton = QtWidgets.QPushButton(Form)
                self.pushButton.setGeometry(QtCore.QRect(10, 20, 271, 71))
                self.pushButton.setObjectName("pushButton")
                self.pushButton.clicked.connect(self.DerivedForm)

                self.retranslateUi(Form)
                QtCore.QMetaObject.connectSlotsByName(Form)

        def retranslateUi(self, Form):
                _translate = QtCore.QCoreApplication.translate
                Form.setWindowTitle(_translate("Form", "Form"))
                self.pushButton.setText(_translate("Form", "导出表格"))

        def toGetData(self):
                global lenlist
                Selection = aoptions[f'{UI.Untility()}']
                city = UI.toObtain()
                t = asyncio.get_event_loop().run_until_complete(main(city, Selection))
                lenlist = []
                for i in t:
                        lenlist.append(i)
                self.tableWidget.setRowCount(len(lenlist))
                la = 0
                for j in lenlist:
                        ListOf = list(j)
                        lenb = 0
                        for x in ListOf:
                                newItem = QTableWidgetItem(x)
                                self.tableWidget.setItem(la, lenb, newItem)
                                lenb += 1
                        la += 1
                return lenlist

        def DerivedForm(self):
                workbook = xlsxwriter.Workbook('住房信息.xlsx')  # 创建一个excel文件
                worksheet = workbook.add_worksheet(u'sheet1')  # 在文件中创建一个名为TEST的sheet,不加名字默认为sheet1

                # worksheet.set_column('A:A', 40)
                # worksheet.set_column('B:B', 20)  # 设置第一列宽度为20像素
                # # bold = workbook.add_format({'bold': True})  # 设置一个加粗的格式对象

                worksheet.write('A1', '房子标题')
                worksheet.write('B1', '地址')
                worksheet.write('C1', '金额')
                worksheet.write('D1', '链接')
                la = 0
                nub = 2
                for j in lenlist:
                        ListOf = list(j)
                        lenb = 0
                        worksheet.write(f'A{nub}', f'{ListOf[0]}')
                        worksheet.write(f'B{nub}', f'{ListOf[1]}')
                        worksheet.write(f'C{nub}', f'{ListOf[2]}')
                        worksheet.write(f'D{nub}', f'{ListOf[3]}')
                        nub = nub + 1
                workbook.close()
                # QMessageBox.information(w2, '提示', '成功')

async def main(Choose,CityUniversity):

        browser = await launch(headless=True, args=['--start-maximized', '--no-sandbox', '--disable-gpu'],
                                                   ignoreDefaultArgs=['--enable-automation'], dumpio=True, slowMo=0.5,
                                                   userDataDir='./cds',executablePath='./chrome-win32/chrome.exe')
        page = await browser.newPage()
        await page.setUserAgent(
                userAgent='Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) '
                                  'Chrome/87.0.4280.66 Safari/537.36 Edg/87.0.664.41')
        await page.setViewport(screen_size())
        await page.goto(
                'https://hz.58.com/xiaoshan/hezu/0/?utm_source=market&spm=u-2d2yxv86y3v43nkddh1.BDPCPZ_BT&PGTID=0d30000a-00bb-c0b5-8f89-ea20b69dca42&ClickID=1')
        await page.evaluate(
                '''() =>{ Object.defineProperties(navigator,{ webdriver:{ get: () => false } }) }''')
        await page.evaluate('''() =>{ window.navigator.chrome = { runtime: {},  }; }''')
        await page.evaluate(
                '''() =>{ Object.defineProperty(navigator, 'languages', { get: () => ['en-US', 'en'] }); }''')
        await page.evaluate(
                '''() =>{ Object.defineProperty(navigator, 'plugins', { get: () => [1, 2, 3, 4, 5,6], }); }''')
        # await page.evaluate('window.scrollTo(0,document.body.scrollHeight)')

        # 这里改动一下就可以爬取更多页数了
        # 有bug还没修复
        # pagenum=10
        await asyncio.sleep(2)
        ast = 0
        num = 2
        await page.click('#commonTopbar_ipconfig > a', button='left')  # 切换城市
        await asyncio.sleep(2)
        await page.type('#selector-search-input', Choose)  # 输入城市
        await asyncio.sleep(2)
        await page.click('#selector-search-btn', button='left')  # 搜索
        await asyncio.sleep(2)
        await page.click(CityUniversity, button='left')
        # await asyncio.sleep(2)
        # await page.click('body > div.main-wrap > div.filter-wrap > div.search_bd > dl.secitem.secitem_fist > dd > div > a:nth-child(2)',button='left')
        await asyncio.sleep(2)
        # await page.click('body > div.list-wrap > div.list-title > a:nth-child(3)', button='left')
        # await asyncio.sleep(2)

        content = await page.content()
        soup = BeautifulSoup(content, 'lxml')
        hls = soup.select('#pager_wrap > div > a:nth-last-child(2) > span')
        pagenum = int(hls[0].text)
        if pagenum == '1':
                print('只有一页')
                return
        else:
                print(f"总共有{pagenum}页")

                # await asyncio.sleep(50)
                titleList = []
                moneylist = []
                addresslist = []
                linklist = []
                while ast <= pagenum:

                        content = await page.content()
                        fontKey = re.search('base64,(.*?)\)', content, re.S).group(1)
                        soup = BeautifulSoup(content, 'lxml')
                        hls = soup.select('div[class="des"]>h2>a[class="strongbox"]')
                        amountOf = soup.select('div[class="money"]>b[class="strongbox"]')
                        address = soup.select('p[class="infor"]>a')
                        Link = soup.select('body > div.list-wrap > div.list-box > ul > li > div.des > h2 > a.strongbox')
                        # for i,j,x,t in zip(hls,amountOf,address,Link):
                        #   print(f'房子标题：{i.text}---地址：{j.text}----金额：{x.text}---链接：{t.get("href")}')

                        for i in hls:
                                jg = i.text
                                jgk = jg.strip()
                                result = get_page_show_ret(jgk, fontKey)
                                # print(result)
                                titleList.append(result)
                        for j in amountOf:
                                money = j.text
                                resultofmoney = get_page_show_ret(money, fontKey)
                                # print(resultofmoney)
                                moneylist.append(resultofmoney)
                        # print(address)
                        for x in address:
                                Address = x.text
                                resultofAddress = get_page_show_ret(Address, fontKey)
                                # print(resultofAddress)
                                # print('====================================================')
                                addresslist.append(resultofAddress)
                        for o in Link:
                                HouseLink = o.get('href')
                                resultofHouseLink = get_page_show_ret(HouseLink, fontKey)
                                linklist.append(resultofHouseLink)

                        # for t in range(len(titleList)):
                        #   worksheet.write(f'A{num}', f'{titleList[t]}')
                        #   worksheet.write(f'B{num}', f'{addresslist[t]}')
                        #   worksheet.write(f'C{num}', f'{moneylist[t]}')
                        #   worksheet.write(f'D{num}', f'{linklist[t]}')
                        #   num = num + 1
                        # worksheet.write(f'E{b}', f'{t.text}')
                        # print(f'房子信息：{titleList[t]}---地址：{addresslist[t]}---金额：{moneylist[t]}\n链接：{linklist[t]}')
                        # print('==========================================================================\n')
                        await page.click('#pager_wrap > div > a.next', button='left')
                        ast = ast + 1
                        await asyncio.sleep(3)
                        pages = await browser.pages()
                        print(f'这是第{ast}页')
                        if len(pages) == 2:
                                pass
                        else:
                                page1 = pages[2]
                                await page1.close()

        print('结束')
        await browser.close()
        return zip(titleList, moneylist, addresslist, linklist)

if __name__ == '__main__':
        aoptions = {'杭州出租': '#tab-default', '个人房源': '#tab-customer', '经纪人': '#tab-middleman', '安选房源': '#tab-ax',
                                '品牌公寓': '#tab-ppgy', '视频房源': '#tab-video', '民宿短租': '#tab-msduanzu', 'vr看房': '#tab-vrhouse'}
        typeList = ['请选择房源类型', '当前城市出租', '个人房源', '经纪人', '安选房源', '品牌公寓', '视频房源', '民宿短租', 'vr看房']
        tblist = ['房子标题', '金额', '地址', '链接']
        app = QApplication(sys.argv)
        w = QMainWindow()
        w2 = QMainWindow()
        TB = Ui_Form2()
        TB.setupUi(w2)
        UI = Ui_Form()
        UI.setupUi(w)
        w.show()
        QMessageBox.about(w,'提示','首次使用可能要下载配置文件有点慢\n开启爬取后进度条不走动因为还没写\n所以可能是未响应的状态实际已经在爬取了\n可以先输入“宁德”测试\n勿做商用，仅供学习\n吾爱破解论坛')
        app.exec_()
```

本来想上传蓝奏云的 可是文件有点大 我就上传到百度云了还希望你们谅解一下
附成品：
链接: https://pan.baidu.com/s/11xh00XEqS4G5yo3BT2xIQw 提取码: cc4m