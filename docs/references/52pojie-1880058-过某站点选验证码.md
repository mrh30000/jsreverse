# 过某站点选验证码

> **作者**: superTian | **发布时间**: 2024-01-13 10:45:00 | **版块**: 『脱壳破解区』 | **查看/回复**: 1289 / 14
> **原文**: [https://www.52pojie.cn/thread-1880058-1-1.html](https://www.52pojie.cn/thread-1880058-1-1.html)

---

#### 一、验证码分析

每次查看数据详情都会弹出验证码，该验证码图片和目标字符都在下面接口返回。

![](https://attach.52pojie.cn/forum/202401/13/103006cbk4bdebbkrkdg9c.png)

点选完之后会请求数据接口，观察入参`ClickText`疑似目标字符的坐标点

![](https://attach.52pojie.cn/forum/202401/13/103059gr7y9dc7o97oxyzy.png)

`Ctrl+Shift+F`搜索一下`ClickText`找到该参数生成位置如下，简单分析了并验证了一下确为目标字符的中心点坐标。验证码本身比较简单，重点关注下怎么实现获取坐标点。

![](https://attach.52pojie.cn/forum/202401/13/103510kphmsgtbmghmdsbp.png)

#### 二、实现方式分析

一顿搜完事发现`ddddocr`在1.3.0版本更新了目标检测功能，[官方介绍如下:](https://github.com/sml2h3/ddddocr)

![](https://attach.52pojie.cn/forum/202401/13/103621nn7gcpplcjgwzmwz.png)

调用`det.detection`方法会返回所有文字的坐标集

![](https://attach.52pojie.cn/forum/202401/13/103701inc5nd4oaq8f88nc.png)

虽然有了所有文字的坐标集，但是我只需要三个目标字符的坐标 并且还要是有序的 怎么办？

我的思路是

1. 先根据识别文字坐标集的顺序 把所有字扣下来 稍作处理后按顺序拼接保存成一张新的图片。
2. 识别处理后的图片得到所有文字。如果不做第一步 直接识别效果不理想。
3. 根据目标文字去匹配该字符的坐标集
4. 根据坐标计算中心点

1、扣字 处理图片：

```
def splicing_strings(image_path: str, boxes: list) -> Image.Image:
    """
    根据坐标集将所有字符处理后拼接为一张新的图片
    :param image_path: 原图地址
    :param boxes: 字符坐标集
    :return:
    """
    original_image = Image.open(image_path)
    new_image = Image.new("RGB", (len(boxes) * 40, 40))  # 40是预设的每个字的宽度，高度

    for idx, box in enumerate(boxes):
        cropped_region = original_image.crop(box)
        cropped_region = cropped_region.convert("L")  # 转换为灰度
        cropped_region = ImageOps.autocontrast(cropped_region)  # 自动调整对比度
        cropped_region = cropped_region.point(lambda x: 0 if x < 128 else 255)  # 二值化处理
        new_image.paste(cropped_region, (idx * 40, 0))

    new_image.save('new_image.jpg')
    return new_image
```

执行完后得到效果如图：

![](https://attach.52pojie.cn/forum/202401/13/103759zle2c2c7mnnzcc9j.png)

2、识别文字

```
ocr = ddddocr.DdddOcr(show_ad=False)
...
# 将splicing_strings的结果丢给ocr.classification
text = ocr.classification(image)
assert len(text) == len(boxes), '识别失败'
print(text)
```

得到结果：`灰庚祸羡浪`

3、根据目标文字去匹配该字符的坐标集
首先获取`目标字符`在`text`中的索引，然后在`坐标集`里面取该索引的坐标集 即为该`目标字符`的坐标集

```
for target in targets:
    x1, y1, x2, y2 = boxes[text.index(target)]
```

4、计算中心坐标

```
points = []
for index, target in enumerate(targets):
    assert target in text, '识别失败'
    x1, y1, x2, y2 = boxes[text.index(target)]
    center_point = (x2 - x1) // 2 + x1, (y2 - y1) // 2 + y1
    points.append(f'{index + 1}:{center_point[0]},{center_point[1]}')
result = '-'.join(points)
```

最后整理下代码成功拿到数据

![](https://attach.52pojie.cn/forum/202401/13/104137t1x7xt5f7k507815.png)
