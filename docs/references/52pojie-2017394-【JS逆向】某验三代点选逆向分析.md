# 【JS逆向】某验三代点选逆向分析

> **作者**: ShriyGo | **发布时间**: 2025-03-21 21:46:00 | **版块**: 『脱壳破解区』 | **查看/回复**: 3513 / 18
> **原文**: [https://www.52pojie.cn/thread-2017394-1-1.html](https://www.52pojie.cn/thread-2017394-1-1.html)

---

*本帖最后由 ShriyGo 于 2025-4-2 19:49 编辑*

## 某验3代点选逆向分析

本文中所有内容仅供学习交流使用，不用于其他任何目的，不提供完整代码，抓包内容、敏感网址、数据接口等均已做脱敏处理，严禁用于商业用途和非法用途，否则由此产生的一切后果均与作者无关.本文章未经许可禁止转载，禁止任何修改后二次传播，擅自使用本文讲解的技术而导致的任何意外，作者均不负责

### 逆向目标

站点：aHR0cHM6Ly93d3cuYmlsaWJpbGkuY29tLw==

目标：某某验点选

#### 抓包分析

提前打开F12，进入网站点击登录，手动点错一次提交后，观察发包：

![](https://attach.52pojie.cn/forum/202503/21/213735hgi1gziv1p00l0p0.png)

这5次请求为核心请求，依次分析。

##### 1. gettype.php

![](https://attach.52pojie.cn/forum/202503/21/213739tv11d6st6ej6klv7.png)

发现其主要参数为`gt`，且能搜索到是来自于`capthca`这个接口，再看看这个接口有什么：

![](https://attach.52pojie.cn/forum/202503/21/213741hl8k9bse5el8kbeb.png)

观察主要返回这些内容，在接下来的流程中可能会用到。

##### 2. get.php

响应内容：返回了很多字段，可能后续有用

![](https://attach.52pojie.cn/forum/202503/21/213743htuq28w2r0wrnzzd.png)

请求参数：`gt`,`challenge`上一步我们已经拿到。`w`看起来似乎要处理。

![](https://attach.52pojie.cn/forum/202503/21/213745m17di41i7diq46f7.png)

##### 3. ajax.php

这里就不贴图了，跟第2步的请求参数一样，可能要处理`w`。至于响应内容，则返回的是当前验证码类型：

```
{"status": "success", "data": {"result": "click"}}
```

##### 4. get.php

响应内容：`pic`返回了图片，以及一些字段后续可能用到

![](https://attach.52pojie.cn/forum/202503/21/213747i1xxc1rqy0x0yx91.png)

请求参数：没有需要特别处理的，此前全都能取到。

![](https://attach.52pojie.cn/forum/202503/21/213749gj000bazbelljeaw.png)

##### 5. ajax.php

响应内容：返回fail，正是点击失败的返回，这一步就是去验证验证码了

```
{
    "status": "success",
    "data": {
        "result": "fail",
        "msg": []
    }
}
```

请求参数：除了`w`，其他参数并无特别

![](https://attach.52pojie.cn/forum/202503/21/213751joaiazll0f57ya5l.png)

#### 流程分析

以上5次请求，发现`2`，`3`，`5`三个步骤中都有w需要处理，目标则为逆向w值。先说结论，实测下来其实`2`，`3`步骤中的w并不校验，本次不分析，着重于分析最后一步提交验证码的w参数分析。

#### 逆向分析w

##### 1. 定位js

先大概看看调用栈，发现都来自于`click.3.1.1.js`这个文件，那目标明确。

![](https://attach.52pojie.cn/forum/202503/21/213754go1ccgtxrgcbglcc.png)

发现有大量这种调用，显然是个混淆。

![](https://attach.52pojie.cn/forum/202503/21/213756nzibv4fbssa03juy.png)

我的习惯是面对这种混淆，优先看看AST还原一部分，便于调试。

##### 2. AST还原

将js拿出来放到vscode收起来观察，4个大方法，一个自执行方法，并不是传统的OB混淆

![](https://attach.52pojie.cn/forum/202503/21/213758m6i8or6irsoiax3a.png)

尝试拿自执行方法中的某个调用，来本地执行打印，发现没问题能正常出值。整个自执行的方法中包含大量`$_CFAn(86)`这种类型调用，结合我们本地能执行的情况，也就是说其实跟OB中的解密函数也差不多一个意思，那就好办了。

> 1. 将unicode编码还原字符串
> 2. 将解密函数调用还原
> 3. 移除无用代码(基于能正常调试了，这一步我没做)

##### 3. debug调试

将还原后的js本地替换浏览器js后，直接搜索参数之一`client_type`可以直接定位到目标，接下来开始繁杂的debug

![](https://attach.52pojie.cn/forum/202503/21/213800dfqmhpk2m6l2shyx.png)

可以看到`w = p + u`，`u = n["$_CDGJ"]()`，`p = w["$_EEn"](h)`，而`h = X["encrypt"](ae["stringify"](o), n["$_CDHy"]())`。

##### 4. u逆向

那就先看u吧。跟进 `n["$_CDGJ"]()`看看。

![](https://attach.52pojie.cn/forum/202503/21/213802edf0zivss21hkfhi.png)

`混淆部分`在视频中有讲理解，就是赋值新变量+concat+取值，由于我们已经AST处理掉了，这一部分就属于无用代码，后续分析其他方法看到这种直接不用管就是。

得益于解混淆后，直接就能跟到：

```
var t = new H()["encrypt"](this["$_CDHy"](e));
```

e是undefined，不用管；

需要再看`new H()["encrypt"]` 和 `this["$_CDHy"](e)`。分别往下看

###### 1. `this["$_CDHy"](e)`

![](https://attach.52pojie.cn/forum/202503/21/213804ln33lszhf78flnz7.png)

跟进来发现主要逻辑有来自于`ve = Q()`，继续往下跟：

![](https://attach.52pojie.cn/forum/202503/21/213806akzssosk22s7a3a2.png)

![](https://attach.52pojie.cn/forum/202503/21/213808kmh4h0qbblmmqzbz.png)

​        结论：

```
this["$_CDHy"](e) = e() + e() + e() + e();

function e() {
    return (65536 * (1 + Math["random"]()) | 0)["toString"](16)["substring"](1)
}
```

###### 2. `new H()["encrypt"]`

![](https://attach.52pojie.cn/forum/202503/21/213811w01yahyhhyrcz1rj.png)

发现这个方法是加在k的原型上的，向上找就能找到`var H = function(){}`

![](https://attach.52pojie.cn/forum/202503/21/213813nhceok7hree4lec7.png)

把整个H拿下来，再把4个方法加上，稍微补一补环境，就能搞定。

![](https://attach.52pojie.cn/forum/202503/21/213815kxm6bccvwmvmf85m.png)

![](https://attach.52pojie.cn/forum/202503/21/213817rmnhnq8usjs8omdp.png)

![](https://attach.52pojie.cn/forum/202503/21/213819td91uwazzpsd6nyd.png)

跟网站生成的值一对比，发现不一样，其实也正常，这个算法是rsa，非对称加密，也可以用标准库去解决试试，反正这里我已经扣下来js了就这样不管了。

到这里，u已经搞定。

##### 5. p逆向

重头戏来了，这里p的组成比较复杂，需要逆向的值有好几个。

![](https://attach.52pojie.cn/forum/202503/21/213821n6s635sminccz6gg.png)

`X["encrypt"](ae["stringify"](o), n["$_CDHy"]())`

p来自于h，那先解决h。观察`n["$_CDHy"]()`的值跟上一步一样，其实也就是一个加密key值，流程如下：

> 1. 先e() + e() + e() + e()生成一个字符串，作为明文来rsa加密
> 2. 接下来的所有需要用到这个明文值的地方，都保持为第一次生成的

所以h的重点在于`X["encrypt"]`和`(ae["stringify"](o)`以及`o`是什么，那就一个个来，先看o

###### 1. o

o的组成很复杂，有这么多东西

> a：其实是点击的坐标，但经过处理
>
> ep：看起来像轨迹和环境
>
> h9s9：可能要逆向
>
> lang、pic：固定或者之前能取到
>
> rp、tt：不知道含义，要逆向

![](https://attach.52pojie.cn/forum/202503/21/213823bhau5dt4dlfofqal.png)

###### 2. `ae["stringify"]`

其实就是JSON.stringify，直接改写就好

###### 3. `X["encrypt"]`

跟进去看看，跟之前u的 `var H = function(){}` 一样，直接把整个`var X = function(){}` 拿出来。这里先不贴图，先往下解决再一起看

![](https://attach.52pojie.cn/forum/202503/21/213825mz1311w5gvhgy515.png)

到此，h就搞定了，接下来看看p的逻辑。

![](https://attach.52pojie.cn/forum/202503/21/214144btgnnodon5benzey.png)

本质上是调用的`this["$_ECS"](e)`

![](https://attach.52pojie.cn/forum/202503/21/214146h2lt5lget2qgl45g.png)

![](https://attach.52pojie.cn/forum/202503/21/214148n5gcynlykqk5lmtm.png)

`$_ECS`是属于`w = {}`，把整个w拿下来，再把之前`var X = function(){}`拿下来，最终js如下：

![](https://attach.52pojie.cn/forum/202503/21/214150n6o5o9c565icpiic.png)

运行后与网站生成的对比，值一致

![](https://attach.52pojie.cn/forum/202503/21/214152ad0juu6u0rdwp1ug.png)

到此，p也搞定。但o中还有好几个参数没处理，继续逆他，累鼠了{:301\_972:} {:301\_972:}

多提一嘴其实这里是aes加密，不过我试过用标准库来解，发现解后的数组跟网站的值对不上，就索性直接扣了，应该是漏了啥，本人对密码学属实不了解

##### 6. o逆向

o有以下字段，实际需要逆向的是a，rp，tt，其他的固定即可，不校验，不过其实也可以跟，大概说下我的理解：

> a:点击坐标，要特殊处理
>
> ep:鼠标移动的全部轨迹，还有一些浏览器事件的环境
>
> h9s9:看其他大佬描述为每天变化的值，实际测下来这个值好几天没变了，固定即可
>
> passtime:点击耗时，随机就行
>
> rp、tt:含义未知，逆他就完事

![](https://attach.52pojie.cn/forum/202503/21/214154viychckukpq2uuk8.png)

###### 1. a

先看看a吧。在w的位置向上看，定位到a的逻辑，发现是参数过来的，那就下断再往前的栈跟

![](https://attach.52pojie.cn/forum/202503/21/214156tlu21mz1509zpza2.png)

向前跟一个栈就能看到`a`其实来自于`n["$_BJGs"]["$_EJC"]()`，继续向前跟。

![](https://attach.52pojie.cn/forum/202503/21/214159x0hk000z0jz2ich7.png)

发现其实来自于`e["$_CEFZ"]`

![](https://attach.52pojie.cn/forum/202503/21/214201athv553386vxys3e.png)

​                搜一下"$\_CEFZ"，发现又来自于t，t在上一个栈，再跟

![](https://attach.52pojie.cn/forum/202503/21/214203wkump4woww3aru03.png)

跟到这里了，下断点刷新在跟

![](https://attach.52pojie.cn/forum/202503/21/214205a49b5r0n03nr94kr.png)

如下分析下：

![](https://attach.52pojie.cn/forum/202503/21/214207o1yigb11y2e9ueob.png)

那就好办了，a的逻辑是这样：

```
var x1 = Math["round"](100 * Number(x));
var y1 = Math["round"](100 * Number(y));
var a = x1 + '_' + y1
```

###### 2. tt

![](https://attach.52pojie.cn/forum/202503/21/214209ueeg7gtj30x0eg09.png)

​                tt方法传参3个，`s = n["$_BHIA"]["$_BGBn"]()`，`r["c"], r["s"]`来自于接口返回，至于s的逻辑就自己跟下好了，跟之前流程一样的

###### 3. rp

rp就是`r["gt"] + r["challenge"] + o["passtime"]`做个md5，可以扣也可以库还原

![](https://attach.52pojie.cn/forum/202503/21/214211jmtkfp2yim2i2pty.png)

到这里，所有参数全部搞定，至于o.ep等之类的，网站自己抓一个写死即可。

#### 验证

没毛病

![](https://attach.52pojie.cn/forum/202503/21/214214fp41mv0vyrzyr088.png)
