# 逆向过云片滑动验证码

> **作者**: 13955925361 | **发布时间**: 2025-09-01 11:19:00 | **版块**: 『脱壳破解区』 | **查看/回复**: 3416 / 17
> **原文**: [https://www.52pojie.cn/thread-2057170-1-1.html](https://www.52pojie.cn/thread-2057170-1-1.html)

---

*本帖最后由 13955925361 于 2025-9-1 11:21 编辑*

首先就是感谢群友带我轨迹算法，经过2天摸索终于给摸出来了。

一半是群友代码，一半是我的小巧思，直接上代码和效果图好吧。

![](https://attach.52pojie.cn/forum/202509/01/111743pymsrgscgvgys4vz.png)

![](https://attach.52pojie.cn/forum/202509/01/111746gc6vars1a6ce7bhe.png)

[Python] *纯文本查看*

```

import random
import requests
import execjs
import json
import re
from captcha_recognizer.recognizer import Recognizer

import math
def jsonp_to_json(jsonp_str):
    """
    将 JSONP 字符串转换为 JSON 对象

    参数:
        jsonp_str (str): JSONP 格式的字符串

    返回:
        dict: 转换后的 JSON 对象
    """
    try:
        # 使用正则表达式提取 JSON 部分
        match = re.search(r'^\w+\(({.*})\)$', jsonp_str)
        if match:
            json_str = match.group(1)
            return json.loads(json_str)
        else:
            # 如果正则匹配失败，尝试直接解析
            return json.loads(jsonp_str)
    except json.JSONDecodeError as e:
        print(f"JSON 解析错误: {e}")
        return None

def get_random_str(t):
    """
    高效生成指定长度的随机字符串（推荐）
    """
    return ''.join(random.choices('abcdefghijklmnopqrstuvwxyz0123456789', k=t))

cc=get_random_str(11)
print(cc)

headers = {
    "Accept": "*/*",
    "Accept-Language": "zh-CN,zh;q=0.9,en;q=0.8,en-GB;q=0.7,en-US;q=0.6",
    "Cache-Control": "no-cache",
    "Connection": "keep-alive",
    "Pragma": "no-cache",
    "Sec-Fetch-Dest": "script",
    "Sec-Fetch-Mode": "no-cors",
    "Sec-Fetch-Site": "same-site",
    "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/139.0.0.0 Safari/537.36 Edg/139.0.0.0",
    "sec-ch-ua": "\"Not;A=Brand\";v=\"99\", \"Microsoft Edge\";v=\"139\", \"Chromium\";v=\"139\"",
    "sec-ch-ua-mobile": "?0",
    "sec-ch-ua-platform": "\"Windows\""
}
cookies = {
    "MEIQIA_TRACK_ID": "315XEmqEg9eE9AmpH78QCVKb717",
    "MEIQIA_VISIT_ID": "315ojXtuJIi1LGvsvUcKlbG0pza",
    "__wksid": "n-46204E3C0DFF4AAE903F207EDB76F9B2",
    "_gid": "GA1.2.1715615459.1756551090",
    "_ga_ESVMH6YSPX": "GS2.1.s1756692044$o12$g0$t1756692044$j60$l0$h0",
    "_ga_B8H0JYR4RL": "GS2.1.s1756692044$o13$g0$t1756692044$j60$l0$h0",
    "_ga": "GA1.1.548472449.1754823427"
}
url = "https://captcha.yunpian.com/v1/jsonp/captcha/get"

exx={
    "browserInfo": [
        {
            "key": "userAgent",
            "value": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/139.0.0.0 Safari/537.36 Edg/139.0.0.0"
        },
        {
            "key": "language",
            "value": "zh-CN"
        },
        {
            "key": "hardware_concurrency",
            "value": 32
        },
        {
            "key": "resolution",
            "value": [
                1464,
                915
            ]
        },
        {
            "key": "navigator_platform",
            "value": "Win32"
        }
    ],
    "nativeInfo": None,
    "additions": {},
    "options": {
        "sdk": "https://www.yunpian.com/static/official/js/libs/riddler-sdk-0.2.2.js",
        "sdkBuildVersion": "1.5.0(2021111001)",
        "hosts": "https://captcha.yunpian.com"
    },
    "fp": "d09f0f0b285c4e6ce19192f567f62bc4",
    "address": "https://www.yunpian.com",
    "yp_riddler_id": "b4cc441f-512c-4d7b-9da1-f6e7c64e2e03"
}

with open("云片.js", "r", encoding="utf-8") as f:
    js_code = execjs.compile(f.read())
ctx=js_code.call("encrypt", exx)

print(ctx)
data = {
    "cb": cc,
     **ctx,
    "captchaId": "974cd565f11545b6a5006d10dc324281"

}
print(data)
response = requests.get(url, headers=headers, cookies=cookies, params=data)
print(response.text)
result = jsonp_to_json(response.text)

bg=result["data"]["bg"]
print(bg)
front=result["data"]["front"]
print(front)
token=result["data"]["token"]
print(token)

bgContent=requests.get(bg,headers=headers).content
frontContent=requests.get(front,headers=headers).content
with open ('bg.png','wb') as f:
    f.write(bgContent)

with open ('front.png','wb') as f:
    f.write(frontContent)

import logging

def get_distance(bg_img):
    """
    使用Recognizer识别滑块验证码的缺口位置并计算滑动距离

    参数:
        bg_img: 滑块背景图片(文件路径或OpenCV图像对象)

    返回:
        计算后的滑动距离(像素)
    """
    # 初始化识别器
    recognizer = Recognizer()

    # 识别缺口位置和置信度
    box, confidence = recognizer.identify_gap(source=bg_img, verbose=False)

    # 记录日志
    logger.debug(f'可信度: {confidence}')

    # 计算滑动距离 (原始代码中的/r544可能是笔误，实际应为/)
    return int(box[0] / 480 * 304) +random.randint(1,2) # 将原始坐标转换为目标距离

# # 配置日志
# logging.basicConfig(level=logging.DEBUG)
logger = logging.getLogger(__name__)

# 使用示例
distance = get_distance("bg.png")
print(distance)
print(f"需要滑动的距离: {distance}像素")

def get_points( distance: int, trajectory_length: int,
               start_x: int = random.randint(800, 850), start_y: int = random.randint(1960, 1971),
               start_time: int = random.randint(56, 60)) :
    """
    生成滑块轨迹数组

    Args:
        distance: 滑块滑行的总距离（像素）
        trajectory_length: 生成轨迹数组的长度（点的个数）
        start_x: 起始x坐标，默认800
        start_y: 起始y坐标，默认1971
        start_time: 起始时间戳，默认56

    Returns:
        List[List[int]]: 轨迹列表，每个元素为 [x, y, time]
    """
    if trajectory_length < 2:
        raise ValueError("轨迹长度至少需要2个点")

    trajectory = []

    # 第一个点
    trajectory.append([start_x, start_y, start_time])

    # 生成中间点
    for i in range(1, trajectory_length - 1):
        # 计算进度比例
        progress = i / (trajectory_length - 1)

        # 使用贝塞尔曲线形式的缓动函数，模拟人手滑动的加速度变化
        # 先快后慢的效果
        eased_progress = 1 - math.pow(1 - progress, 3)

        # 计算x坐标（基于缓动进度）
        base_x = start_x + int(distance * eased_progress)

        # 添加小幅随机偏移，模拟手部微抖
        x_jitter = random.randint(-1, 1)
        x = base_x + x_jitter

        # 确保x坐标单调递增（允许偶尔相等）
        if x <= trajectory[i - 1][0]:
            x = trajectory[i - 1][0] + random.randint(0, 2)

        # y坐标轻微抖动，主要在1971-1973之间
        y_offset = random.choice([0, 0, 0, 1, 1, 2])  # 偏向于较小的偏移
        y = start_y + y_offset

        # 时间递增，模拟真实的时间间隔
        # 根据原数据分析，时间间隔通常在6-200ms之间，大部分在10ms以内
        if i < trajectory_length * 0.7:  # 前70%的点，间隔较小
            time_delta = random.randint(6, 25)
        else:  # 后30%的点，间隔可能较大（模拟末尾的停顿）
            time_delta = random.randint(8, 200)

        time = trajectory[i - 1][2] + time_delta

        trajectory.append([x, y, time])

    # 最后一个点，确保距离精确
    final_x = start_x + distance
    final_y = start_y + random.randint(0, 2)
    final_time = trajectory[-1][2] + random.randint(8, 50)

    trajectory.append([final_x, final_y, final_time])

    # 验证距离是否正确
    actual_distance = trajectory[-1][0] - trajectory[0][0]
    assert actual_distance == distance, f"距离不匹配: 期望{distance}, 实际{actual_distance}"

    return trajectory

def reduce_points( t):
    # 原始点数据
    # 如果点数小于等于50，直接返回
    if len(t) <= 50:
        return t

    # 初始化结果列表，包含第一个点
    e = [t[0]]
    # 获取最后一个点
    n = t[-1]
    # 计算步长
    i = len(t) // 50  # Python中使用//进行整数除法
    # 如果步长小于2，返回原始列表
    if i < 2:
        return t
    # 按照步长选取点
    r = 1
    while r < len(t) - 2:
        e.append(t[r])
        r += i
    # 添加最后一个点
    e.append(n)
    return e

distanceX = (304 - 59) * (distance / (304 - 42)) / 304
orginal_points =get_points(distance,int(distance * 0.68))
print(orginal_points)
points = reduce_points(orginal_points)
print(points)

metadata = {
            "points": points,
            "distanceX": distanceX,
            "fp": "d09f0f0b285c4e6ce19192f567f62bc4",
            "address": "https://www.yunpian.com",
            "yp_riddler_id": 'b4cc441f-512c-4d7b-9da1-f6e7c64e2e03'
        }

dd=js_code.call("encrypt", metadata)
print(dd)

datalist = {
    "cb":get_random_str(11) ,
     **dd,
    'token':token,
    "captchaId": "974cd565f11545b6a5006d10dc324281"

}
print(datalist)
def parse_data(datalist):
    headers = {
        "Accept": "*/*",
        "Accept-Language": "zh-CN,zh;q=0.9,en;q=0.8,en-GB;q=0.7,en-US;q=0.6",
        "Cache-Control": "no-cache",
        "Connection": "keep-alive",
        "Pragma": "no-cache",
        "Sec-Fetch-Dest": "script",
        "Sec-Fetch-Mode": "no-cors",
        "Sec-Fetch-Site": "same-site",
        "User-Agent": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/139.0.0.0 Safari/537.36 Edg/139.0.0.0",
        "sec-ch-ua": "\"Not;A=Brand\";v=\"99\", \"Microsoft Edge\";v=\"139\", \"Chromium\";v=\"139\"",
        "sec-ch-ua-mobile": "?0",
        "sec-ch-ua-platform": "\"Windows\""
    }
    cookies = {
        "MEIQIA_TRACK_ID": "315XEmqEg9eE9AmpH78QCVKb717",
        "MEIQIA_VISIT_ID": "315ojXtuJIi1LGvsvUcKlbG0pza",
        "__wksid": "n-46204E3C0DFF4AAE903F207EDB76F9B2",
        "_gid": "GA1.2.1715615459.1756551090",
        "_ga_ESVMH6YSPX": "GS2.1.s1756692044$o12$g0$t1756692044$j60$l0$h0",
        "_ga_B8H0JYR4RL": "GS2.1.s1756692044$o13$g0$t1756692044$j60$l0$h0",
        "_ga": "GA1.1.548472449.1754823427"
    }

    url_1 = "https://captcha.yunpian.com/v1/jsonp/captcha/verify"
    response = requests.get(url_1, headers=headers, cookies=cookies, params=datalist)
    data_2 = response.text
    return data_2

response_2= parse_data(datalist)
print(response_2)

```

**JS代码**

[JavaScript] *纯文本查看*

```

const CryptoJS = require("crypto-js");
const crypto = require('crypto')

getRandomStr = function(t) {
        for (var e = ""; e.length < t; )
            e += Math.random().toString(36).substr(2);
        return e = e.slice(0, t)
    }

function rsaEncrypt(data, publicKey) {
    try {
        // 标准化公钥格式
        let formattedKey = publicKey.trim();

        // 如果是单行格式，重新格式化
        if (!formattedKey.includes('\n') && formattedKey.startsWith('-----BEGIN PUBLIC KEY-----')) {
            const keyContent = formattedKey
                .replace('-----BEGIN PUBLIC KEY-----', '')
                .replace('-----END PUBLIC KEY-----', '')
                .replace(/\s/g, '');

            const chunks = keyContent.match(/.{1,64}/g) || [];
            formattedKey = [
                '-----BEGIN PUBLIC KEY-----',
                ...chunks,
                '-----END PUBLIC KEY-----'
            ].join('\n');
        }

        const buffer = Buffer.from(data);
        const encrypted = crypto.publicEncrypt({
            key: formattedKey,
            padding: crypto.constants.RSA_PKCS1_PADDING
        }, buffer);

        return encrypted.toString('base64');
    } catch (error) {
        console.error('RSA encryption failed:', error.message);
        // 返回原始数据作为备选方案
        return Buffer.from(data).toString('base64');
    }
}

encrypt = function(data) {
    try {
        const dataStr = JSON.stringify(data);
        const aesKey = getRandomStr(16);
        const iv = getRandomStr(16);
        const combinedKey = aesKey + iv;
        const publicKey = "-----BEGIN PUBLIC KEY-----MIGfMA0GCSqGSIb3DQEBAQUAA4GNADCBiQKBgQDnOWe/gs033L/2/xR3oi6SLAMPBY5VledUqqH6dbCNOdrGX4xW+1x6NUfvmwpHRBA2C7xWDDvOIldTl0rMtERTDy9homrVqEcW6/TY+dSVFL3e2Yg2sVaehHv7FhmATkgfC2FcXt8Wvm99QpKRSrGKpcFYJwOj2F8hJh+rTG0IPQIDAQAB-----END PUBLIC KEY-----";

        const encryptedData = CryptoJS.AES.encrypt(dataStr, CryptoJS.enc.Latin1.parse(aesKey), {
            iv: CryptoJS.enc.Latin1.parse(iv),
            mode: CryptoJS.mode.CBC,
            padding: CryptoJS.pad.Pkcs7
        }).toString();

        const encryptedKey = rsaEncrypt(combinedKey, publicKey);

        return {
            i: encryptedData,
            k: encryptedKey
        };
    } catch (error) {
        console.error("Encryption failed:", error);
        throw new Error("Encryption process failed.");
    }
};

//
// e={
//     "browserInfo": [
//         {
//             "key": "userAgent",
//             "value": "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/139.0.0.0 Safari/537.36 Edg/139.0.0.0"
//         },
//         {
//             "key": "language",
//             "value": "zh-CN"
//         },
//         {
//             "key": "hardware_concurrency",
//             "value": 32
//         },
//         {
//             "key": "resolution",
//             "value": [
//                 1464,
//                 915
//             ]
//         },
//         {
//             "key": "navigator_platform",
//             "value": "Win32"
//         }
//     ],
//     "nativeInfo": null,
//     "additions": {},
//     "options": {
//         "sdk": "https://www.yunpian.com/static/official/js/libs/riddler-sdk-0.2.2.js",
//         "sdkBuildVersion": "1.5.0(2021111001)",
//         "hosts": "https://captcha.yunpian.com"
//     },
//     "fp": "d09f0f0b285c4e6ce19192f567f62bc4",
//     "address": "https://www.yunpian.com",
//     "yp_riddler_id": "b4cc441f-512c-4d7b-9da1-f6e7c64e2e03"
// }
//
//
// a=encrypt(e)
// console.log(a);

```
