# 猿人学第二题之获取 滑块图片相关信息

> **作者**: JackLSQ | **发布时间**: 2025-06-12 20:26:00 | **版块**: 『编程语言区』 | **查看/回复**: 1878 / 5
> **原文**: [https://www.52pojie.cn/thread-2038410-1-1.html](https://www.52pojie.cn/thread-2038410-1-1.html)

---

*本帖最后由 JackLSQ 于 2025-6-12 20:27 编辑*

网站地址：aHR0cHM6Ly9tYXRjaDIwMjUueXVhbnJlbnh1ZS5jbi9tYXRjaDIwMjUvdG9waWMvMg==

### 解混淆

唉，题目混淆的看着眼睛疼，得用ast进行解混淆。简单的解一下变量名混淆和数字计算的就可以。
解混淆得代码如下所示:

```
const parser = require("@babel/parser");
const generator = require("@babel/generator").default;
const traverse = require("@babel/traverse").default;
const types =require("@babel/types");

const fs = require("fs");

let codeData=fs.readFileSync("./yrx.js",'utf-8');

const code =`    if (yrx_L8 < -2283 + -2782 + 5070) {
    var yrx_L9 = function (yrx_L6) {
      var yrx_L7 = yrx_L5 || 569 + 241 * -5 + 636;
      for (;;) return;
    };
    yrx_L8 += 3 * 2599 + 9 * 70 + -8426;
  } else
      yrx_L8 -= 1959 + -2 * -4037 + -10029;
`;

const ast = parser.parse(codeData);

// 递归计算表达式的函数
function evaluateExpression(node) {
    if (node.type === 'NumericLiteral') {
        return node.value;
    }
    if (node.type === 'UnaryExpression' && node.operator === '-') {
        return -evaluateExpression(node.argument);
    }
    if (node.type === 'BinaryExpression') {
        const left = evaluateExpression(node.left);
        const right = evaluateExpression(node.right);
        switch (node.operator) {
            case '+': return left + right;
            case '-': return left - right;
            case '*': return left * right;
            default: throw new Error(`不支持的运算符: ${node.operator}`);
        }
    }
    return null;
};

const visitor={

        Identifier(path){
            let node =path.node;
            if(node.name && !(node.name.indexOf("yrx_"))){
                cnt++
                //console.log("name ==>",node.name,"name len==>",node.name.length);
                path.node.name="yrx_"+"L"+node.name.length;
            }
        },
        BinaryExpression(path){
            const isConstant = (node) => (
                node.type === 'NumericLiteral' ||
                (node.type === 'UnaryExpression' && node.operator === '-' && isConstant(node.argument)) ||
                (node.type === 'BinaryExpression' && isConstant(node.left) && isConstant(node.right))
            );

            if (isConstant(path.node)) {
                const value = evaluateExpression(path.node);
                if (value !== null) {
                    // 用 NumericLiteral 替换表达式
                    path.replaceWith({
                        type: 'NumericLiteral',
                        value: value
                    });
                }
            }
        }
};

traverse(ast,visitor);

const result = generator(ast);

console.log(result.code);

fs.writeFileSync("./yrx2.js",result.code,'utf-8');
```

建议在解混淆traverse得时候，尽量一个对象的一个对象的traverse。每解一次就本地写一次文件，保存上次解出的结果。再解的时候就从上次开始解。

### 本地替换调试

他这个本地替换调试有个坑，代码中有格式检测，需要对代码进行压缩之后再进行本地替换。

在解码之后的代码中搜索参数mmc,就能定位到加密的位置。
如下所示

```
                        ((yrx_L12["data"] = {
                          mmc:
                            Array["from"](yrx_L11)
                              ["map"](function (yrx_L6) {
                                var yrx_L7 = yrx_L5 || 0;
                                for (;;)
                                  return yrx_L6["toString"](16)["padStart"](
                                    2,
                                    "0"
                                  );
                              })
                              ["join"]("") + yrx_L9,
                        }),
                        4))
```

然后在这个位置附近下断点，让网页断在这附近，后面就是简单的跟栈，搜索变量名。很快就能定位到。

过程就不详细描述了，简单的说一下它这个参数是怎么来的。
首先 随机生成了一个长度为16的字符串，然后将其转换为byte数组。
然后就是拿浏览器的ua和服务器的时间(这里有个坑 它再Date 下面塞了东西，然后从服务器上拿的时间戳)，拼成一个字符串，对其转byte数字。
再然后把长度16的byte数组和124的byte数组进行一个运算得到一个新的byte数组长度是124。
最后把这个新的数组转换成字符串。

下面是mmc 生成的js代码

```
yrx_L9 = Array["apply"](null, Array(16))["map"](function() {
        for (; ; )
                return ("0123456789abcdef0123456789abcdef0123456789abcdef6789abcdef789abcdef6789abcdef")["charAt"](Math["floor"](Math["random"]() * 62))
})["join"]("")

//console.log("yrx_L9==>",yrx_L9);

yrx_L10 = new TextEncoder()["encode"](yrx_L9)

//console.log("yrx_L10==>",yrx_L10);

let ua ='Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/137.0.0.0 Safari/537.36';

let tm ="__TIME__";  //这个是向服务发起请求  https://match2025.yuanrenxue.cn/match2025/logo 这个地址 get 请求  返回值

let str =ua+tm;

let temp = new TextEncoder()["encode"](str)

//console.log("temp ",temp);

var yrx_L13 = function(yrx_L8, yrx_L9) {
        var yrx_L12 = 27;
        for (;;)
  if (yrx_L12 < 16) {

                if (yrx_L12 < 8)
      yrx_L12 < 4 ? yrx_L12 < 2 ? yrx_L12 < 1 ? ((yrx_L13[yrx_L15] = 255 - yrx_L15), (yrx_L12 += 14)) : (yrx_L12 += ((yrx_L17 = 0), 21)) : yrx_L12 < 3 ? yrx_L20 < yrx_L9["length"] ? (yrx_L12 += 9) : (yrx_L12 += 21) : (yrx_L12 += 23) : yrx_L12 < 6 ? yrx_L12 < 5 ? (yrx_L12 += 9) : (([yrx_L13[yrx_L17], yrx_L13[yrx_L16]] = [yrx_L13[yrx_L16], yrx_L13[yrx_L17], ]), (yrx_L12 -= 1)) : yrx_L12 < 7 ? ((yrx_L19 = 0), (yrx_L12 += 24)) : (yrx_L12 += ((yrx_L16 = (yrx_L16 + yrx_L13[yrx_L19]) & 255), 1));
                else {
                        if (yrx_L12 < 12)
        yrx_L12 < 10 ? yrx_L12 < 9 ? (([yrx_L13[yrx_L19], yrx_L13[yrx_L16]] = [yrx_L13[yrx_L16], yrx_L13[yrx_L19], ]), (yrx_L12 += 23)) : ((yrx_L16 = (yrx_L16 + yrx_L13[yrx_L17] + yrx_L14[yrx_L17]) & 255), (yrx_L12 -= 4)) : yrx_L12 < 11 ? ((yrx_L15 = 0), (yrx_L12 += 11)) : ((yrx_L19 = (yrx_L19 + 1) & 255), (yrx_L12 -= 4));
                        else {
                                if (yrx_L12 < 14)
          yrx_L12 < 13 ? (yrx_L12 -= ((yrx_L14 = new Uint8Array(256)), 2)) : (yrx_L12 += (yrx_L17++, 9));
                                else {
                                        if (yrx_L12 < 15)
            yrx_L12 += ((yrx_L14[yrx_L15] = yrx_L8[yrx_L15 % yrx_L8["length"]]), 6);
                                        else
            return yrx_L18
                                }
                        }
                }
        } else {
                if (yrx_L12 < 24)
      yrx_L12 < 20 ? yrx_L12 < 18 ? yrx_L12 < 17 ? ((yrx_L18 = new Uint8Array(yrx_L9["length"])), (yrx_L12 -= 10)) : (yrx_L20++, (yrx_L12 -= 15)) : yrx_L12 < 19 ? (yrx_L12 += (yrx_L15++, 3)) : ((yrx_L20 = 0), (yrx_L12 -= 17)) : yrx_L12 < 22 ? yrx_L12 < 21 ? (yrx_L12 -= 2) : yrx_L15 < 256 ? (yrx_L12 -= 21) : (yrx_L12 -= 18) : yrx_L12 < 23 ? yrx_L17 < 256 ? (yrx_L12 -= 13) : (yrx_L12 += 3) : (yrx_L12 -= 8);
                else {
                        if (yrx_L12 < 28) {
                                if (yrx_L12 < 26)
            yrx_L12 < 25 ? (yrx_L12 -= ((yrx_L13 = new Uint8Array(256)), 12)) : (yrx_L12 -= 9);
                                else {
                                        if (yrx_L12 < 27)
            yrx_L12 -= ((yrx_L16 = 0), 25);
                                        else {
                                                var yrx_L13, yrx_L14, yrx_L15, yrx_L16, yrx_L17, yrx_L18, yrx_L19, yrx_L20, yrx_L21;
                                                yrx_L12 -= 3
                                        }
                                }
                        } else
        yrx_L12 < 30 ? yrx_L12 < 29 ? (yrx_L12 += ((yrx_L18[yrx_L20] = yrx_L9[yrx_L20] ^ yrx_L21), 1)) : (yrx_L12 -= 12) : yrx_L12 < 31 ? ((yrx_L16 = 0), (yrx_L12 -= 11)) : ((yrx_L21 = yrx_L13[(yrx_L13[yrx_L19] + yrx_L13[yrx_L16]) & 255]), (yrx_L12 -= 3))
                }
        }
};

let yrx_L11 = yrx_L13(yrx_L10,temp);

//console.log("yrx_L11==>",yrx_L11);

let mmc =Array["from"](yrx_L11)["map"](function(yrx_L6) {
  for (; ; )
      return yrx_L6["toString"](16)["padStart"](2, "0")
})["join"]("") + yrx_L9;

console.log("mmc==>",mmc );
```

### 从服务器中获取滑块图片

```
import requests
import subprocess
import os

headers = {
        'accept': '*/*',
        'accept-language': 'zh-CN,zh;q=0.9',
        'referer': 'https://match2025.yuanrenxue.cn/match2025/topic/2',
        'user-agent': 'Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/137.0.0.0 Safari/537.36',

}

def getTime():

    response = requests.get('https://match2025.yuanrenxue.cn/match2025/logo', headers=headers)

    print(response.text)
    return response.text

def getCaptchaJpg(tm:str):
    jsCode=""
    with open("./gameing.js",mode="r",encoding="utf-8") as fp:
        jsCode =fp.read()

    with open("./run2.js",mode="w",encoding="utf-8") as fp:
        fp.write(jsCode.replace("__TIME__",tm))

         # js文件最后必须有输出,我使用的是 console.log
    pro = subprocess.run("node run2.js", stdout=subprocess.PIPE)

    os.remove("run2.js")
    # 获得标准输出
    _token = pro.stdout
    # 转一下格式
    token = _token.decode().strip()
    res = token[token.find('>')+1:]
    res=res.replace(' ','');
    print(str(res))
    print(len(res))

    data = {
    'mmc': res,
    }
    response = requests.post(
        'https://match2025.yuanrenxue.cn/match2025/topic/2_captcha_jpg',

        headers=headers,
        data=data,
    )
    print(response.content)

if __name__ == "__main__":
   getCaptchaJpg(getTime())
```

最终效果

![](https://attach.52pojie.cn/forum/202506/12/202254fqoc8kmqzgzwbogp.jpg)

后面的图片缺口验证
平坦流有点恶心，眼睛都看肿了，等后面有时间再看，到时候给它解一下平坦流，再去调了。
