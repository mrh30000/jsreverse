# 【JS逆向】某土地市场网逆向分析

> **作者**: littlewhite11 | **发布时间**: 2025-03-04 15:32:00 | **版块**: 『脱壳破解区』 | **查看/回复**: 4209 / 37
> **原文**: [https://www.52pojie.cn/thread-2011345-1-1.html](https://www.52pojie.cn/thread-2011345-1-1.html)

---

*本帖最后由 littlewhite11 于 2025-3-10 07:29 编辑*

## 逆向目标

* 网址：`aHR0cHM6Ly93d3cubGFuZGNoaW5hLmNvbS8jLw==`
* 目标：反调试及请求头`Hash`

## 反调试

进入网站，我们会发现`F12`无法打开控制台。那就换一种方式，以谷歌浏览器为例`设置 -> 更多工具 -> 开发者工具`，然后网页就会跳到空白页了，这是除了`debugger`外一种经典的反调试手段。

那么我们如何过掉这个反调试呢？目前，我遇到的就两种情况，第一种就是`window.close()`，第二种就是`location.href = ''`，还有其他情况的话欢迎各位大佬补充。

下面直接开始分析：

我们用无痕浏览器分析（因为在`Script`断点时不会加载插件的JS代码），打开控制台，勾上`Script`断点，输入网址，成功断住。

![](https://attach.52pojie.cn/forum/202503/04/152953qdpdeiipi2isup2s.png)

然后我们先搜索`window.close`，`F8`一直走，之后某个文件出现了相关代码，直接下断。

![](https://attach.52pojie.cn/forum/202503/04/152955ywmmwuu2277m7pkk.png)

然后放开`Script`断点，直接`F8`过掉，成功在断点处停住。

![](https://attach.52pojie.cn/forum/202503/04/152957gd4q7o1g1b7xtbr9.png)

我们往前跟栈，看网站通过什么手段进行的反调试。可以看到，是`e > 10 * this.maxPrintTime`这个条件成立才触发`this.onDevToolOpen`，从而触发后续的`window.close`

![](https://attach.52pojie.cn/forum/202503/04/153000mpyryyzyzx4o0rrv.png)

那我们就看看代码具体做了什么，我们在前面的`e`和`r`处下断，重新请求，成功断住。

![](https://attach.52pojie.cn/forum/202503/04/153002nqagooucrc9r1r9m.png)

然后我们单步跟进去`m`函数，可以看到，先取了一下时间戳，然后调用传进的参数，最后再返回参数调用的时间差，那猫腻肯定是出现在传入的函数中了。

![](https://attach.52pojie.cn/forum/202503/04/153004rfssu0aan3pkuhsh.png)

我们直接进入被调用的参数函数中，可以看到`t.largeObjectArray`是一个大对象，`b`其实是`console.table`函数，也就是在控制台打印一个大对象。

![](https://attach.52pojie.cn/forum/202503/04/153006mli2xs3ih3x6hli3.png)

那反调试的逻辑大体清楚了：如果打开了控制台，就会通过`console.table`打印一个很大的对象，这就需要消耗时间，后续`e > 10 * this.maxPrintTime`就成立，就会进入最终`window.close`的逻辑。

我们可以通过`hook`，返回一个固定的时间戳来过掉反调试（油猴或者直接在控制台注入都可以）。

```
Date.prototype.getTime = function(){
    return 1741067137856
}
```

`hook`掉时间戳之后，我们会发现控制台一直会被清空，也不利于调试，我们直接把清空控制台的代码置为空函数`E = function(){}`。

![](https://attach.52pojie.cn/forum/202503/04/153008q3td3mbztdkmjzkt.png)

同理，再把和控制台打印相关的`b`和`w`函数也置空（在对应函数断住的时候去置空）。

![](https://attach.52pojie.cn/forum/202503/04/153010l1nl98y22ly8l3g1.png)

最后，能够在控制台输入并输出表示我们成功过掉了反调试。

![](https://attach.52pojie.cn/forum/202503/04/153013b1bedqsmj9eeoems.png)

## 抓包分析

我们直接点击`土地供应 -> 供地计划`，会发一个`list`的包，请求头中的`Hash`参数就是需要逆向的参数。

![](https://attach.52pojie.cn/forum/202503/04/153015u3gpcb136p4j6n9b.png)

## 逆向分析

一般请求头参数加密，我们可以通过`hook`的手段去找加密位置（可以去找现成的`hook`代码）。我选择直接从启动器去找了，一般都会看异步之前的`request`。

![](https://attach.52pojie.cn/forum/202503/04/153017x46zlf0x50555f55.png)

下断，发包，然后找请求拦截器。

![](https://attach.52pojie.cn/forum/202503/04/153019pk97q7c2zcd3m8oh.png)

很显然，下图大概率就是加密参数的生成位置。

![](https://attach.52pojie.cn/forum/202503/04/153021rr2sn7q54nnr75rc.png)

我们在控制台输出一下结果，可以看到密文的长度为`64`，看起来很像`SHA256`。

![](https://attach.52pojie.cn/forum/202503/04/153024hrgzpswyww4ep76r.png)

那我们就去尝试一下，看是否为标准的`SHA256`，字符串`'1'`的摘要为`6b86b273ff34fce19d6b804eff5a3f5747ada4eaa22f1d49c01e52ddb7875b4b`。

![](https://attach.52pojie.cn/forum/202503/04/153026kc719rll111ygtul.png)

再去控制台对字符串`'1'`进行摘要，经对比，算法是标准的，没有魔改。

![](https://attach.52pojie.cn/forum/202503/04/153028cqv2y2j2bj9m94z8.png)

那加密过程大概清楚了，对`userAgent`、`今天的日期（日）`以及`list`进行`SHA256`得到`Hash`。

![](https://attach.52pojie.cn/forum/202503/04/153030m0tq0ywnw2knmdzm.png)

那我们直接对接口进行请求模拟。

![](https://attach.52pojie.cn/forum/202503/04/153032sxh0shd7ha7xe976.png)

成功！！！
