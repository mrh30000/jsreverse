# 【JS逆向】某点数据登录逆向分析

> **作者**: littlewhite11 | **发布时间**: 2025-05-03 14:00:00 | **版块**: 『脱壳破解区』 | **查看/回复**: 6211 / 41
> **原文**: [https://www.52pojie.cn/thread-2028837-1-1.html](https://www.52pojie.cn/thread-2028837-1-1.html)

---

*本帖最后由 littlewhite11 于 2025-5-3 17:57 编辑*

## 逆向目标

* 网址：`aHR0cHM6Ly9hcHAuZGlhbmRpYW4uY29tL2xvZ2luP2xpbms9aHR0cHMlM0ElMkYlMkZhcHAuZGlhbmRpYW4uY29tJTJGcmFuayUyRmlvcyUyRg==`
* 目标：登录参数逆向

## 抓包分析

我们选择邮箱，输入账号密码进行登录，发了一个`login`请求，其中参数`k`是需要逆向分析的，请求头和响应都不需要额外处理。

![](https://attach.52pojie.cn/forum/202505/03/135228llpwl5imniuxvi44.png)

因为参数`k`并没有什么特征，所以直接搜索不太现实，所以我们选择跟栈来看参数如何生成的。

从启动器入手，看到了`Promise.then`，那参数的生成有可能涉及异步，那我们在异步之后下断点，验证一下猜想。

![](https://attach.52pojie.cn/forum/202505/03/135230if0z9psgs1gkf901.png)

可以看到异步之后参数`k`已经生成，那大概率就涉及异步了。

![](https://attach.52pojie.cn/forum/202505/03/135232ppqztkpv7sw67qwt.png)

## 逆向分析

既然知道可能涉及异步，那我们就在异步之前下断，可以找一找请求拦截器。

![](https://attach.52pojie.cn/forum/202505/03/135234j70lqiazh1fi5rw1.png)

![](https://attach.52pojie.cn/forum/202505/03/135236av2yyyhd2yx2ml2y.png)

可以看到`data`中参数`k`还没有生成。

![](https://attach.52pojie.cn/forum/202505/03/135238lc8gqzqxgapu1ikc.png)

那我们可以简单`hook`一下参数`k`（这里因为只剩下参数`k`还没有`set`，且只是为了找到位置，所以`hook`代码就简单写了，更完善的`hook`代码可以自行查资料实现）。

```
Object.defineProperty(e.data, 'k', {
    set(){
        debugger
    }
})
```

`hook`代码生效后，过掉断点，发现参数`k`的`set`并没有`hook`到，那么根据经验，`data`可能在过程中被重新赋值了，所以我们`hook`一下`data`的`set`方法。

```
Object.defineProperty(e, 'data', {
    set(){
        debugger
    }
})
```

成功`hook`到`data`设置参数。

![](https://attach.52pojie.cn/forum/202505/03/135240cy0k0zyo2j5127y5.png)

往上跟一个栈，我们就可以发现参数`k`的生成位置了。

![](https://attach.52pojie.cn/forum/202505/03/135243qs7oefsqeq7bqzzs.png)

把断点下好，重新刷新网页（因为之前的`hook`代码写得很潦草，防止出现不期待的情况），然后进行参数`k`的逆向。

在逆向过程中，有很多小技巧可以简化我们的逆向过程，比如这里的`t.data`，在经过`h()(t.data, !1) || {}`其实并没有变化，那我们就可以不去抠`h()`的代码。

![](https://attach.52pojie.cn/forum/202505/03/135245r7f3uppx3pxzyj77.png)

然后我们的重点是`Object(y.a)`这个函数，直接跟进去，`e`是`data`，`path`是路径参数`/v1/user/company/login`，`n`就是前文的`n`，`r`是请求方法。

![](https://attach.52pojie.cn/forum/202505/03/135247cgoggnraddw8b7lg.png)

可以直接把这个函数拉下来本地，然后缺什么补什么，还是那句话，能简化的东西就简化，能不抠的代码就不要去抠。

这里的`t`经过`c()`函数后并没有发生变化，那就可以不抠`c()`的代码，直接`var n = t;`

![](https://attach.52pojie.cn/forum/202505/03/135251e7zldyivvr9rgwyh.png)

这里的`!l()(n)`可能只是在校验`n`这个对象是什么类型，在生成参数`k`这一细分流程中，`n`始终是对象类型，那我们也没必要去抠`l()`的代码，直接把条件当`true`处理。

![](https://attach.52pojie.cn/forum/202505/03/135253posq545qu12z81np.png)

`l()`函数代码

![](https://attach.52pojie.cn/forum/202505/03/135255r19hzlflw8wxthp8.png)

然后就是加密方法`Object(_.b)`，我们直接跟进去。可以清晰地看到是`aes-cbc`的加密，但我们常遇到的是用`crypto-js`这个库实现的，这里显得有些陌生。

![](https://attach.52pojie.cn/forum/202505/03/135257t5cf5cecv29ehfef.png)

遇事不决，那就问一问AI，可以看到AI也是很给力，给出了一版答案。

![](https://attach.52pojie.cn/forum/202505/03/135259sd5odofo0fuyt1rz.png)

![](https://attach.52pojie.cn/forum/202505/03/135302mezte2pihlxr4a4z.png)

我们本地模拟实现，和网站的结果对比一下，可以看到结果是正确的。

![](https://attach.52pojie.cn/forum/202505/03/135304pl1bzyhrih66c1t0.png)

![](https://attach.52pojie.cn/forum/202505/03/135306inlh12cv1vl242z2.png)

那我们的代码就算是抠完了。

那么有人就好奇，在进行`aes`加密时用到的几个参数，看起来并没有规律，那为什么我们并没有去跟，其实这都是经验之谈。

> 服务器拿到我们生成的加密参数，是需要去校验的，对于`aes`这种对称加密算法而言，它的`key`和`iv`肯定在服务器中存有一份，既然`login`接口没有上报，那`key`和`iv`要么是固定的，要么是服务器下发的，这个网站每次刷新`key`和`iv`都会变，那大概率是服务器下发的，我们可以直接搜索。

可以看到这些参数确实在`html`页面中（服务器下发）。

![](https://attach.52pojie.cn/forum/202505/03/175504x1cxsjq1qfxsqq18.png)

那我们模拟登录的流程就是：先请求`html`页面拿到`s、k和l`三个参数，然后通过前文的流程生成参数`k`，最后携带生成的参数模拟请求。

模拟请求结果：

![](https://attach.52pojie.cn/forum/202505/03/135310l4gcpncgpnexbxze.png)

成功！！！
