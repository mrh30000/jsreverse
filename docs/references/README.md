# Web JS 逆向参考资料索引

来源：52pojie 精华/技术/热门/最新帖 + 关键词搜索（`hubcli 52pojie`）。
文件名格式：`52pojie-<帖子ID>-<标题>.md`（批量归档）与 `<帖子ID>-<英文 slug>.md`（早期人工命名），
正文即帖子 Markdown（含作者 / 发布时间 / 原文链接头部）。

- 早期归档：**43 篇**（原 `README` 表格，人工筛选+英文 slug）
- 批量归档：**166 篇**（2026-09-20 自动抓取+筛选，见第二节）
- 补充归档（一）：**82 篇**（2026-09-20 扩关键词补漏抓取，见第三节）
- 补充归档（二）：**41 篇**（2026-09-20 第三轮长尾关键词 + 筛选口径修正，见第四节）
- 补充归档（三）：**30 篇**（2026-09-20 第四轮长尾关键词 [webpack/protobuf/字体反爬] + 审计补漏，见第五节）
- 补充归档（四）：**15 篇**（2026-09-20 第五轮长尾关键词 + 审计补漏 [sign 参数逆向类漏拣]，见第六节）
- 补充归档（五）：**60 篇**（2026-09-20 第六轮 [验证码/滑块/纯算/参数分析类被 `search --limit 40` 截断的老帖补回] + STRONG 扩容，见第七节）
- 补充归档（六）：**13 篇**（2026-09-21 第七轮 [「验证码逆向专栏」系列 + 点选/指纹类] + 修一处历史误杀，见第八节）
- 补充归档（七）：**22 篇**（2026-09-21 第八轮 [STRONG 扩容后存量补捞 + 剔除 23 篇「通用加解密」假阳性]，见第九节）
- 补充归档（八）：**18 篇**（2026-09-21 第九轮 [隐称「某X」关键词首次覆盖 + 审计 B/C 表逐条补漏]，见第十节）
- 补充归档（九）：**27 篇**（2026-09-21 第十轮 [审计 C 表 STRONG 整类漏拣补回 + 长尾关键词/隐称二批 + 坑19 缓解落地]，见第十一节）
- 补充归档（十）：**22 篇**（2026-09-21 第十一轮 [关键词「视频/m3u8/直播源 × 解密」与「平台+登录/接口」两个组合命中存量老帖 + 审计 C 表逐条补回]，见第十二节）
- 补充归档（十一）：**110 篇**（2026-09-21 第十二轮 [`hubcli search` GBK 字符集失效修复 → 浏览器驱动搜索 + 全量分页；30 个高价值历史词复扫命中 88 篇被 `limit 40` 截断的存量老帖]，见第十三节）
- 补充归档（十二）：**37 篇**（2026-09-22 第十三轮 [历轮全部 586 个历史关键词「浏览器全量分页」复扫至 470 词收口；新增全部来自审计 C/E 表补漏通道，主题集中为 `视频/m3u8/直播源解密` 与 `登录/接口/签名/参数逆向` 两类存量老帖]，见第十四节）
- 补充归档（十三）：**39 篇**（2026-09-22 第十五轮[第十四轮审计粗筛出的 548 篇 C/E 表候选**逐条抓正文复核**：548 → 196 核心命中 → 定稿 39，主题为 `登录/参数/算法还原` 17 + `扣代码/AST/反调试` 6 + `风控/字体/指纹` 7 + `视频/m3u8/DRM` 9]，见第十五节）
- CF 盾纯协议实现方案调研（CSDN）：**25 篇**（2026-09-22 第十六轮，见第十六节）
- 补充归档（十四）：**8 篇**（2026-09-22 第十六轮[审计 **B/G 表首次正文级复核** + 关键词通道见底确认，主题为`浏览器扩展/Electron 逆向` 2 + `前端反调试` 2 + `JS 侧滑块/签名` 1 + `接口逆向` 3]，见第十七节）
- 补充归档（十五）：**14 篇**（2026-09-22 第十七轮[**新增「全站未归档命中」扫描通道**（`jsrev_scan17.txt` 555 条 → 人工挑 120 条抓正文）+ 审计 B/D/E/G/C 五表 186 条正文复核；主题为 `登录/接口协议` 5 + `参数/签名算法还原` 5 + `webpack 扣取/补环境` 2 + `滑块 JS 侧` 1 + `响应媒体解密` 1]，见第十八节）
- 补充归档（十六）：**22 篇**（2026-09-23 第十八轮[「全站未归档命中表」**全量重建 + 标题级自动排序**（1982 命中 → 抓 502 篇正文复核）+ 技术词盲区补充批（jsc/electron/小程序/AST）；主题为 `小程序与 H5 前端逆向` 9 + `参数·算法还原（含补环境）` 5 + `验证码·滑块（JS 侧）` 2 + `字体 / m3u8 / jsc` 5 + `协议` 1 + `AST 工具` 1]，见第十九节）
- 补充归档（十七）：**14 篇**（2026-09-23 第十九轮[「全站未归档命中表」**第二批分层取样**：技术词白名单 114 + 系列通道 12 + score 6~7 新面孔 14 + score 3~5 核心词 477（共 617）+ 「NOISE 过滤误杀体检批」173，合计抓 769 篇正文复核 → 定稿 14；主题为 `Electron/asar 逆向` 4 + `视频/m3u8/DRM` 3 + `网站 JS 加密与响应解密` 3 + `字体/协议/反调试` 3 + `登录 JS` 1]，见第二十节）
- 补充归档（十八）：**38 篇**（2026-09-23 第二十轮[**新开「版块枚举」通道**：`Thread --url forum-<fid>-<page>.html` 逐页枚举 7 个站点版块（脱壳破解区/编程语言区/原创发布区/动画发布区/软件调试区/逆向资源区/安全工具区）共 8881 唯一主题，其中 **7668 篇是历轮「关键词搜索 + 四类策展榜」从未覆盖的池外老帖**（池 17770 → 25442）；再叠「web 专属词 − 桌面/native 词」净化 → 抓 178 篇正文复核 → 定稿 38；主题为 `视频/m3u8/TS 帧/DRM` 8 + `签名·参数·响应加解密` 11 + `小程序与 WebView` 6 + `JSVMP/Electron/asar/wasm` 4 + `前端反调试·扩展·水印` 4 + `其他 web 侧` 5]，见第二十一节）
- 补充归档（十九）：**27 篇**（2026-09-23 第二十一轮[**「版块枚举」通道加深**：7 个版块从 30 页翻到 150 页 + 新开 4 个未枚举版块（`22` UnPackMe/CrackMe、`8` 悬赏问答、`68` 教学培训、`75` 脱壳破解讨论求助），共 **44467 唯一主题**，其中 **32792 篇（74%）是池外新帖**（池 25442 → **58239**）；再叠「具体逆向信号 + web 侧上下文 − 求助/native」三层标题门禁 → 抓 256 篇正文复核 → 定稿 27；本轮同时修掉一处**系统性去重 BUG**（坑 56：已归档 ID 正则吃掉首位数字，致 256 候选里 **32 篇其实是已归档老帖**）；主题为 `视频/m3u8/DRM` 4 + `前端逆向·JS hook·AST` 6 + `参数/签名/接口逆向` 6 + `Wasm·流式响应` 2 + `反爬·Cookie·风控` 3 + `编码/混淆还原` 2 + `工具与工程` 3 + `教程方法论` 1]，见第二十二节）
> 目录现状（`artifacts/52pojie-fetch21/` 终检，2026-09-23 第二十一轮）：
> 文章 `.md` 共 **887 个**（不含本 README）= `52pojie-<id>-<标题>.md` **805 个** + 早期 `<id>-<slug>.md` **51 个**
> + 无 ID 文件 **6 个**（其他平台批量归档 `csdn-*` **25 个**，见第十六节；另 6 个为早期手工文件：`weixin-*.md`、`cloudflare*.md`、`verified.md` 等）。
> 唯一帖子 ID **854** 个 ⚠️（**本轮修正**：第二十轮记的「852」是坑 56 的错误正则得出的，实际当时为 827；本轮 +27 = 854）。
> 重复 ID **0** 组（仅 2 组 `.验证报告.md` 与主文共用 ID：`2098573`、`2100076`，非重复）。
> 空文件（<400B）**0** 个；薄文件（<1500B）**31** 个（均为历轮存量）。
> 编码扫描（`repair-encoding.py --scan`）888 文件 **1 命中**（`1985027` 内含站点原帖的混淆代码，不可还原、非抓取问题）。

---

## 二十二、补充归档（十九）（27 篇，2026-09-23 第二十一轮：**版块枚举加深 + 一处系统性去重 BUG 修复**）

**本轮的判断：第二十轮证明了「版块枚举」是唯一还在产的通道，并留下两条明确的待办 ——
① 把每个版块从 30 页加深到 100~150 页；② 补枚举未覆盖的版块。本轮两条都做了，
但真正的收获其实是一处把「已归档」算错的正则 BUG。**

### 一、版块枚举加深（本轮唯一在产通道）

```
board-sweep.py --out boards21 --maxpage 150 --fids 5,24,2,6,59,4,41   # 7 版块：30 → 150 页
board-sweep.py --out boards21 --maxpage  80 --fids 22,8,68,75          # 新开 4 版块
rebuild_board21_pool.py                                                # 从页文件重建全版块合并池
```

| 版块 | fid | 本轮真实页数 | 唯一主题 | 说明 |
| --- | --- | --- | --- | --- |
| 脱壳破解区 | 5 | **150+**（仍在增长） | 7499 | 未到底，下轮可继续加深 |
| 编程语言区 | 24 | **150+**（仍在增长） | 7498 | 同上 |
| 原创发布区 | 2 | **150+**（仍在增长） | 7493 | 同上 |
| 逆向资源区 | 4 | 106 | 5275 | 已到底 |
| 动画发布区 | 6 | 60 | 2961 | 已到底 |
| 悬赏问答区 | 8 | 80+ | 3993 | 新开，问答噪声多 |
| UnPackMe/CrackMe | 22 | 80+ | 3996 | 新开，native 为主 |
| 脱壳破解讨论求助区 | 75 | 80+ | 3997 | 新开，求助为主 |
| 软件调试区 | 59 | 21 | 1017 | 已到底 |
| 安全工具区 | 41 | **8** | 379 | 已到底（小版块） |
| 教学培训区 | 68 | **8** | 359 | 已到底（小版块） |

**结果：44467 唯一主题，池外 32792 篇（74%）**，全量池 25442 → **58239**。

### 二、新坑 55：版块翻到末页「回卷第 1 页」，不是返回空

`animation`/`安全工具区`/`教学培训区` 三个版块实际只有 8~60 页，
但旧脚本只在「返回**空**列表」时熔断 —— 而 Discuz 超页后会**返回第 1 页内容的复制**，
于是 `安全工具区` 白抓 **142 页**（p9~p150 全是同一条 `thread-1859777`）。
**修复**＝`board-sweep.py` 增加「**连续 2 页零新增 ID**」熔断（进度行现在打印 `(+新增数)`）：

```python
added = sum(1 for x in d if tid_of(x["url"]) not in rows)
stale = stale + 1 if added == 0 else 0
if stale >= 2: break        # 已到末页，站点回卷第 1 页
```

⇒ 判「翻到底」的判据是**新增数归零**，不是「返回空」。`Thread --page N` 同理。

### 三、新坑 56（本轮最重要的修复）：已归档 ID 正则会**吃掉首位数字**

历轮（含 skill 的 `jsrev-scan.py`）用 `^[a-z0-9]*-?(\d{5,})-?.*\.md$` 提取已归档帖子 ID。
问题出在 `[a-z0-9]*` —— **它包含数字**，贪婪匹配会把文件名开头的数字一起吃掉：

| 文件名 | 错误结果 | 正确结果 |
| --- | --- | --- |
| `2080428-shortvideo-sig3-fingerprint-pure-algo.md` | `80428` | `2080428` |
| `2066191-websocket-reverse-rebuild-html.md` | `66191` | `2066191` |

⇒ **全部 51 个早期 `<id>-<slug>.md` 文件的 ID 提取都是错的**，已归档集合因此漏掉 **49 个 ID**；
后果是这些老帖会被重复抓取、重复归档（`52pojie-2080428-*.md` 与 `2080428-*.md` 并存）。

**本轮实测影响面**：256 条候选里 **32 条其实是已归档老帖**（`2082553` 某御验证码、`2123850` 某 q 音乐
jsvmp、`2106217` iv8、`2066191` WebSocket… 全是历轮已归档的「名帖」，所以命中表每轮都把它们捞回来）。

**修复**＝文中第一个长度 ≥5 的数字串即帖子 ID：

```python
m = re.search(r"(\d{5,})", f)     # ✅ 正确
# m = re.match(r"^[a-z0-9]*-?(\d{5,})-?.*\.md$", f)   # ❌ 会吃掉首位数字
```

⚠️ **同时修正历轮 README 的计数**：第二十轮 README 写的「唯一帖子 ID 852」正是这个错误正则的产物，
**实际当时是 827**；本轮修正后为 **854**（827 + 本轮 27）。此后各轮计数应以新正则为准。

### 四、口径：候选三层门禁（本轮第一版失败过一次）

`build_probe21.py` 第一版把「裸 `插件`/`扩展`/`小程序`」当成 web 逆向信号 →
T1 的 807 条里大半是 **悬赏问答区的「求 XX 插件」「找 XX 课程」**（微信插件、PS 插件、word 翻译插件）。
新版三层门禁（全部必须通过）：

| 门禁 | 作用 | 例 |
| --- | --- | --- |
| `HARD` | 必须含**具体**逆向/加密/协议信号 | 逆向/加密/签名/参数/接口/滑块/webpack/hook… |
| `STRONG2`（T2~T4） | 必须有**具体逆向动作或算法/防护名**，泛词单独出现不算 | 加密/签名/滑块/补环境/混淆/扣代码/m3u8/token… |
| `WEBCTX` | 必须有 **web 侧上下文** | js/web/网页/前端/小程序/接口/api/http/m3u8… |
| `NOISE` | 排除求助/资源/教程句式 | 求/求助/悬赏/推荐/教程/课程/资料/下载/版本/怎么… |
| `NATIVE` | 排除 native/移动/游戏 | VMP(`vmp` 裸词)/壳/UPX/KeyGenMe/CM/IDA/x64dbg/APK/微信 PC… |

效果：命中表 3362 → **256 条候选**（`noise 10182 / native 8970 / nohard 15750 / noctx 1219 / weak 760`）。
**教训：裸「插件/扩展/小程序/接口」是 web 词，不是「逆向」词**（与坑 19/49 同源：口径一刀切会静默引入整类噪声）。

### 五、本轮流程

```
thread_pull21.py    → Thread 四类策展榜（506 唯一 / 池外 10 / 未归档 432）→ web 逆向新增 0
board-sweep.py ×2   → 11 版块枚举（44467 唯一，池外 32792）
rebuild_board21_pool.py → 合并池（**注意：分两批跑时后一批会覆盖 board_pool.json，必须重建**）
merge_pool21.py     → pool_v21.json（58239）
jsrev-scan.py       → 未归档命中表 3153（score≥6: 240）
build_probe21.py    → 三层门禁 → 256 候选
probe21.py          → 抓正文 240 ok / 8 perm / 5 empty / 1 antibot
manifest21.py       → 重建本轮清单 + 标记 32 条「已归档重复」
score21.py          → v2 打分（剔除 32 条重复后 134 条核心命中）
review21.py         → 紧凑复核表（附 fences/ASK/NOLINK/NOIMPL/THIN 信号）
→ 逐条复核（四条收录判据）→ kept_final21.json（27）
materialize21.py    → docs/references/52pojie-<id>-<标题>.md
finalize21.py       → BLOCK_EXTRA21.json（213 条判废固化）
```

### 六、本轮主题分布（27 篇）

| 主题 | 篇数 | 代表 |
| --- | --- | --- |
| **视频 / m3u8 / DRM / 媒体** | 4 | `1305472` 解析站 js+m3u8 链路、`1939582` WEB 前端定位 ts 后处理代码、`1939664` 某瓜视频文件解密、`609243` Chrome CDM DRM 缺陷 |
| **前端逆向 · JS hook · AST** | 6 | `1519460` js hook 改进（遍历加密命名函数）、`1519457` 通杀型 js hook 入门、`1487207` WebSocket send 定位、`1744420` ast-hook-for-js 修复、`1530460` 猿人学第二题混淆对抗、`1772986` 某 Excel 前端组件逆向 + JSHook |
| **参数 / 签名 / 接口逆向** | 6 | `1484457` 直播心跳签名（5 次 hash + wasm）、`1803311` 塔科夫市场 API、`1883017` 某加解密网站（sojson host/data 头）、`955693` 商家后台 rohrToken、`1752766` 前程无忧 HmacSHA256 签名、`1735114` 中烟新商盟登录（`F(F(b)+c)`） |
| **Wasm · 流式响应** | 2 | `1934571` WebAssembly 逆向工程简介（导出表/胶水 js 断点）、`1781712` 某 GPT 网站问答流程（stream 流）逆向复现 |
| **反爬 · Cookie · 风控** | 3 | `1616585` A 站连环变动 cookie（`__cf_bm`）、`1606904` 某站 captcha 机器人检测原理、`1592049` 某网站响应加密数据解密 |
| **编码 / 混淆还原** | 2 | `1690132` 前端逆向「社会主义核心价值观」加密、`1565072` 某房产网混淆 js 分析与还原 |
| **工具与工程** | 3 | `1959338` WEB 前端逆向 JS RPC（含 spa2 / TS PES NALU 案例）、`1730501` 抓包逆向谷歌翻译 API、`1553861` asar unpack 细节 |
| **教程 / 方法论** | 1 | `1217467` 网络逆向（一）油猴脚本与抓包初步 |

### 七、本轮审计与统计

- **Thread**：四类策展榜 506 唯一（池外仅 10 条，全为资源/求助帖）→ **web 逆向新增 0**（第 3~21 轮同一结论）。
- **关键词**：**未投新词**（第 16~18 轮已证明复合词命中率≈0）。
- **池扩容**：`pool_v20.json` 25442 → **`pool_v21.json` 58239（+32797）**。
- **去重**：本轮修正坑 56 后，候选与已归档集完全对齐；落盘 27 篇 `have` 命中 **0**。
- **编码**：`repair-encoding.py --scan` 888 文件 **1 命中**（`1985027` 站点原帖自带，可还原行 0，不修不丢）。
- **终检**：文章 `.md` **887 个**（`52pojie-` **805** / 早期 slug **51** / 无 ID **6** / csdn **25**），
  唯一 ID **854**，空文件（<400B）**0**，薄文件（<1500B）**31**，
  重复 ID 仅 2 组已知 `.验证报告.md`（`2098573`、`2100076`，非重复）。

### 下轮建议

1. **版块枚举仍未吃尽**：`脱壳破解区`/`编程语言区`/`原创发布区` 三版块到 150 页**仍在增长**，
   直接加大 `--maxpage`（如 300~500 页）即可继续产；`悬赏问答区`/`UnPackMe`/`脱壳破解讨论求助区`
   刚开到 80 页，也可加深。判据用**新增数归零**（坑 55）。
2. **本轮 27 篇里 100% 来自 T4（版块枚举池外新帖）**，命中表 T1/T2/T3 只贡献了重复项 ——
   说明「命中表」通道对**已归档名帖**的召回已经饱和（每轮都在重捞同一批），
   增量只能靠**结构性枚举**。
3. **坑 56 修复后，建议下轮开工先跑一次「重复体检」**：
   检查 `docs/references/` 中是否存在「同 ID 两个文件」（slug 版 + `52pojie-` 版）——
   若历轮真有误重复归档，应在此一并清理。
4. 未枚举版块仍可补：`65` 移动安全区、`32` 病毒分析区（预计 web 逆向极低，优先级最低）。
