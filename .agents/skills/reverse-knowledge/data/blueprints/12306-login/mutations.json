{
  "mutations": [
    {
      "name": "验证码要填序号不是坐标",
      "rule": "交互输入的是验证码序号（逗号分隔），由 get_point() 映射成坐标串",
      "symptom_if_wrong": "captcha-check 的 result_code 不为 4"
    },
    {
      "name": "验证码图片是 base64",
      "rule": "captcha-image64 返回的 image 字段需 base64 解码后才可保存为 jpg",
      "symptom_if_wrong": "保存的图片打不开"
    },
    {
      "name": "token 链式依赖",
      "rule": "uamauthclient 的 tk 必须取 uamtk 返回的 newapptk",
      "symptom_if_wrong": "token 校验失败"
    }
  ]
}
