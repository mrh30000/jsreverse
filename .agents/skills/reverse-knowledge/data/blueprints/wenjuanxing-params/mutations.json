{
  "mutations": [
    {
      "name": "abcdx 反调试门禁",
      "rule": "abcd2/abcd4/abcd5 开头都有 if(!abcdx()) return;，命中 webdriver / $cdc_ / PhantomJS / callPhantom 时函数直接返回 undefined",
      "symptom_if_wrong": "jqParam 与 jqsign 变 undefined 或空值"
    },
    {
      "name": "ktimes 作 XOR 密钥",
      "rule": "b = ktimes % 10，且 b == 0 时置为 1",
      "symptom_if_wrong": "jqsign 与服务端不一致（ktimes 若为 10 的倍数必须按 1 算）"
    },
    {
      "name": "starttime 格式与取反",
      "rule": "starttime 需 replace(/-/gm,'/') 后 new Date()；且若秒级时间戳 %10 > 0，要把时间戳字符串反转后再拼 '89123'",
      "symptom_if_wrong": "jqParam 不一致"
    },
    {
      "name": "activityId 先异或",
      "rule": "页面里的 activityId 需先 DecodeId（^2130030173）再取 parseInt 参与拼串",
      "symptom_if_wrong": "jqParam 不一致"
    },
    {
      "name": "jqpram 要二次编码",
      "rule": "jqParam 需 encodeURIComponent 后作为 jqpram 拼进 URL",
      "symptom_if_wrong": "提交失败"
    },
    {
      "name": "jcn 可省略",
      "rule": "作者实测「有没有都行」",
      "symptom_if_wrong": "无影响"
    },
    {
      "name": "调试地址可绕 debug 处理",
      "rule": "把 ks.wjx.top/vj/xxxxx.aspx 改成 ks.wjx.top/vm/xxxxx.aspx（vj 会做 debug 处理，vm 不会）",
      "symptom_if_wrong": "调试时反复被 debugger 打断"
    },
    {
      "name": "get_jqParam 可复用版的返回值截断点（少做一次 abcd5 循环左移）",
      "rule": "源文 1361387 贴出的**可复用** `get_jqParam()` 里是 `return jqParam`（位置在 `abcd3` 之后、`abcd5` 之前，且 `var _0x5d90fd = abcd5(jqParam);` 的结果**从未回写**）；而页面内联代码是 `jqParam = _0x5d90fd`（在 `abcd5` 之后）。⇒ 照抄可复用版本会**少做一次循环左移**",
      "symptom_if_wrong": "长度对但内容错位、服务端判参数非法（少做 abcd5 的循环左移 ⇒ 字符完全一样、顺序不同）"
    },
    {
      "name": "_0x3a5cf2 是数字相加不是字符串拼接",
      "rule": "`var _0x3a5cf2 = _0xd16fcc + _0x4aaf4a + parseInt(activityId);` —— 三项都是 `number`（`_0xd16fcc = parseInt(时间戳串 + '89123')`、`_0x4aaf4a = abcd1(...)` 的数值、`parseInt(activityId)`）⇒ JS 左结合下是**算术和**",
      "symptom_if_wrong": "写成 Python 字符串拼接（把三个数当字符串连起来）⇒ 进制编码前的整数与浏览器不一致 ⇒ 最终 jqParam 全错（注意区分：字符串 '89123' 的拼接发生在 `_0xd16fcc` 内部、是 `parseInt` 的入参，不要与这一处混淆）"
    }
  ]
}
