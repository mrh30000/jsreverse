## 入口与传输

- 生成位置: 函数 k()（文章称其为该加密算法的入口函数）
- 触发时机: 搜索请求前
- 传输方式: 请求参数（文章只说是「请求的参数」，字段位于 query 还是 body 需看截图，正文未文字化）

## 排查步骤

1. Fiddler 抓包搜索请求，确认四个字段，其中第一个是随机加密串
2. F12 → 全局搜索关键词 crawlerInfo，定位到 2 处匹配的文件
3. 在两处匹配处下断点，点击搜索触发断点
4. 跟栈最终进入 k() 函数，k() 即加密入口
5. 按葫芦画瓢还原 k()（文章细节在视频里，无文字步骤）

## 算法口径

- 家族: `unknown`
- 细节: 文章未给出任何算法细节（正文以 Fiddler/Chrome 截图为主，算法讲解在配套视频里）；仅可确定为「JS 加密运算而成」。

## 明确留白（原文未给出，不要臆测）

- algorithm 完全未知：正文只有截图，算法在被引用的视频教程里（evidence: screenshot-only）
- 其余三个参数名文章未列出（只在截图中）
- crawlerInfo 的长度、字符集、编码方式、是否 URL 编码全部未知
- 是否含时间戳/随机数/环境指纹未知
- 文章标记为 historical 的依据：样本为 2019-01，且目标站点为拼夕夕旗下「夕夕进宝」，作者未声明仍有效
