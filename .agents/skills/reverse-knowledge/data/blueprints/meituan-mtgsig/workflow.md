## 入口与传输

- 生成位置: VMP（先 AST 解混淆，把 b 函数解出来；再层层插桩）
- 触发时机: 请求前生成
- 传输方式: header（文章标题称 mtgsig1.2；正文未逐字给出 header 名）

## 排查步骤

1. AST 解混淆，先把 b 函数解出来（文章：'非常简单'）
2. 补环境（作者补了一天，说明代码量非常之大）
3. VMP 层层入栈处插桩：ki+1, kg['getUint8'](ki+1), kb，保存打印日志
4. a3：搜索 dfpID 位置，扣完 a3 即生成
5. a5：先定位 'kR["apply"](kS, kP)'（数组转字符串），再插桩找到 0..255 数组，用 JSON.stringify 分析；推导 (acc+i+arr[i]+31) mod 256 轮询
6. a6：按日志插桩，'缺啥扣啥'
7. 最后把 a3/a5/a6 拼成 mtgsig；找关键校验接口测试（作者用某Booking 测试）

## 算法口径

- 家族: `custom`
- 细节: VMP。a5 可见轮询：acc = (acc_prev + i + encrypt_arr[i % 16] + 31) % 256；另有 0..255 动态数组与 16 位数组参与；数组由 kr = kR["apply"](kS, kP) 生成。a3/a6 未给出可读算法。
- 密钥/常量: `encrypt_arr = [152, 232, 186, 109, 125, 188, 249, 229, 95, 58, 250, 230, 169, 75, 115, 56]`
- 输出编码: unknown
- 输出长度: 476（文章：'这里正常应该是476位'）

## 自算核对（源文章数字重算结果）

- 按 acc=(acc_prev + i + encrypt_arr[i%16] + 31) mod 256 重算 i=0..16，得 [183,191,154,41,201,169,199,210,88,186,221,237,193,56,216,62,5]。其中 i=0..15 依次为 183/191/154/41/.../62，与文章给出的 183、191、154、41、...(第16个)62 完全一致。
- 由上，文章第 17 个'未给结果'应为 261 % 256 = 5（文章写成 318 % 256 是笔误）。
- encrypt_arr 长度核算 = 16，与'需要与之运算的16位数组'一致；第 17 个用 encrypt_arr[0]=152，符合 i%16 轮询。

## 来源之间的矛盾

- 52pojie-1964578:144（'297 - 256 || 297 * 256'） vs 52pojie-1964578:136（同一步骤上下文 '410 - 256 || 410 % 256'） —— 第 4 步写的是 '297 * 256'，但按上下文应为取模 '% 256'（297-256=41，也即 297%256=41）（判定: 显然笔误：应为 '297 % 256'。按作者给出的结果 41 反推亦成立。）
- 52pojie-1964578:158-161（'第17个 78+152  62+16  152 第1个数组 / 230 + 31 / 261 - 256 || 318 % 256'） vs 52pojie-1964578:152-155（第16个用 '318 % 256'） —— 第 17 步算式为 261，但右侧写成 '318 % 256'（沿用了上一步的 318），且未给结果（判定: 笔误：应为 '261 % 256' = 5。我用递推核算得第 17 个结果 = 5。）

## 明确留白（原文未给出，不要臆测）

- a3 的算法（只说'扣完 a3 即生成'，无源码）。
- a6 的算法（'瞎JB一顿乱找和乱猜' + 截图）。
- mtgsig 最终拼接格式（a3/a5/a6 如何拼、有无分隔符、编码）。
- header 名逐字（文章标题是 mtgsig1.2，正文未逐字给出 'mtgsig' 头名）。
- a8 与 d1 两段（文章明确'待续'）。
- dfpID 的来源与生成方式。
