## 入口与传输

- 生成位置: 0f35c990.js（文件名每天 0 点-1 点间变动一次）
- 触发时机: 页面加载时自执行生成；请求时作为 cookie 发出
- 传输方式: cookie，参数名逐字："__zp_stoken__"；另有 window['s']

## 排查步骤

1. 定位文件：抓包看到首次使用 __zp_stoken__ 的请求，回溯到 0f35c990.js（每晚 0-1 点变动）
2. 识别两段：上面一小段自执行函数（控制变量 G 为常量 16），下面部分控制变量由入参决定
3. AST 构建 map 映射表：把每个 be 值对应的执行内容映射出来；be=10768 → 得 be=10410 → 继续（去掉无用 break）
4. 以 return 和 be=undefined 为界，模拟整条执行流；常量合并（如 _='len'; e='gth'; y=_+e → 'length'）
5. 还原出可读代码，再扣成纯算；找到 token 生成入口（如 be=5553），用自建代码依次调用
6. 注意把函数内部修改但不返回的数组手动返回

## 算法口径

- 家族: `custom`
- 细节: 文章未给出最终算法族（几乎全是截图，evidence=screenshot-only）。可见特征：控制流平坦化 VMP，需 AST 建映射表；字符串编码方式为'长度 + 逐字符 ASCII 码'拼接（如 '0b92' → 48,98,57,50，4=长度）。
- 输出编码: unknown
- 输出长度: 不固定（文章：'怎么生成的结果会变，甚至长度都不是固定的'）

## 自算核对（源文章数字重算结果）

- ASCII 核算：'0b92' → ord('0')=48, ord('b')=98, ord('9')=57, ord('2')=50，与文章 48/98/57/50 完全一致。
- 种子串长度核算：len('booubZKQSgODivfbCd8dHDdK8T7BITcNqfge/Jfh7Oo=') = 44，与文章'44代表该种子的长度'一致。

## 明确留白（原文未给出，不要臆测）

- 最终 token 的完整算法（文章正文几乎全是截图，无源码，关键步骤 evidence=screenshot-only）。
- be→Ee/Ae/Ne 的'固定算法'具体内容。
- token 输出的编码与长度（文章只说长度不固定）。
- ASCII 拼接片段在整体算法中的位置。
