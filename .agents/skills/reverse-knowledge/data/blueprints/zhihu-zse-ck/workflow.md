## 入口与传输

- 生成位置: 最外层混淆 js → eval 胶水代码 → window.Go/run 初始化 → wasm；最终加密函数 sub_988；主循环 runtime.run$1$gowrapper，子函数组 (*main.VM)
- 触发时机: 无痕模式访问知乎页面（如提问题主页）时返回验证中间页，加载该 js 生成 ck
- 传输方式: cookie，名 __zse_ck（标题逐字；正文出现的是 __g["ck"]）

## 排查步骤

1. 无痕访问目标页，确认返回验证中间页并加载那段混淆 js
2. ob 混淆下搜 `>> 0x13` 下断点看 this（大数组长度 80、字符串数组长度 16；注意下标 13 的字符串长 7094）
3. 反汇编 jsvmp，看到 __g["ck"]=假值 与 window["eval"](胶水代码)
4. 胶水代码会绑定到 window.Go；在 wasm 加载前下断点看 window.Go
5. 在 syscall/js.valueGet 下断点看 wasm 从 js 取了什么；堆栈会不断出现 (*main.VM)
6. 在 runtime.run$1$gowrapper 看主循环；地址 0x11428、长度 15160 处 base64 解码得 VM 字节码
7. 反汇编 wasm VM（大数组 2329、字符串数组 90），在 new Function 内发现第二层 jsvmp（大数组 8000+，全为环境检测）
8. 最终在 sub_988 确认魔改 SM4 + 魔改 base64，用 python 还原对拍（中文无乱码）

## 算法口径

- 家族: `symmetric`
- 细节: 框架为 SM4 但被不同程度魔改，再经一个魔改过的 base64 编码形成 ck。外层 ob 混淆 + jsvmp → eval 胶水代码加载内嵌(base64) wasm → wasm 内再跑一个自实现 VM(jsvmp 结构) → 输出加密环境信息 → 解密/校验/拼接明文 → sub_988 加密
- 输出编码: 魔改 base64（原文：'经过一个魔改过得base64编码'）
- 输出长度: null（原文未给；两个默认假 ck 实测均为 112 字符，见 self_check）

## 自算核对（源文章数字重算结果）

- 左篇第 54 行默认 ck 实测长度 112（含 003_ 前缀），第 79 行默认 ck 实测长度 112（含 003_ 前缀）；两者去掉 4 字符前缀后主体均 108、且可被 4 整除，但第 79 行无 '=' 填充、且含 '+' '/' → 与『魔改 base64』说法相容
- \xE2\x80\x8D 三字节按 UTF-8 解码 = U+200D ZERO WIDTH JOINER（不可见分隔符）

## 来源之间的矛盾

- 52pojie-1968832:105 vs 52pojie-1970885:94 —— 左篇称旧版加载 wasm 后需调用【_encrypt】才得 ck、新版没有了该步骤；右篇称最终加密函数是 sub_988（判定: 非真矛盾，属版本迭代：旧版入口 _encrypt，2024 版入口为 sub_988；两文均未给 _encrypt 与 sub_988 是否同一逻辑，无法判定）

## 明确留白（原文未给出，不要臆测）

- SM4 的密钥/IV/轮数等 key_material 原文未给（且为魔改，标准密钥不可套）
- 魔改 base64 的字母表原文未给
- 明文 6 段的具体内容与顺序原文未给（仅说 6 部分、\xE2\x80\x8D 分割）
- 如何从 wasm 提取 0x11428/长度 15160 那一坨字节码、反汇编器细节，原文只在图里
- jsvmp 中『>> 19』与『>> 0x13』的具体分组含义原文未展开
- 左右篇文件仅约 5KB，绝大多数关键信息在 30+ 张 52pojie 附件截图里，未能进入文本抽取
