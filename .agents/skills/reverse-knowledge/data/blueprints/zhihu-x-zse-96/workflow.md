## 入口与传输

- 生成位置: 未定位（文章未给生成函数名/文件；仅给算法与作者 github 还原源码链接）
- 触发时机: 请求前（搜索接口 /search_v3 携带）
- 传输方式: header，名疑为 x-zse-96（来自标题；原文未逐字给出请求头写法）

## 排查步骤

1. 抓 /api/v4/search_v3 请求，确认 x-zse-96 与 source 实际形态
2. 按原文构造 source（101_3_2.0 + 路径query + 疑似cookie）
3. md5(source) 取 hex，末尾补 \u0000
4. 从末尾每 3 个 charCode 一组（[32,31,30]、[29,28,27] ...）
5. 套用 4 种位运算之一得 m，按 fixed_str[m%64]/(m>>6)%64/(m>>12)%64/(m>>18)%64 查表
6. 拼接各组 4 字符得最终签名；参考作者 github：skygongque/Spider 09-知乎x-zse-96/main.py

## 算法口径

- 家族: `custom`
- 细节: MD5 + 自定义位运算 + 64 字符查表。source→md5(hex 32)→末尾补 \u0000 → 从末尾倒序每 3 个 charCode 一组 → 4 种位运算之一得 m → fixed_str[m%64],fixed_str[(m>>6)%64],fixed_str[(m>>12)%64],fixed_str[(m>>18)%64] 输出 4 字符/组
- 密钥/常量: `fixed_str = "RuPtXwxpThIZ0qyz_9fYLCOV8B1mMGKs7UnFHgN3iDaWAJE-Qrk2ecSo6bjd4vl5"`
- 输出编码: 自定义字母表
- 输出长度: 44（自算 33/3*4；文章未给，属推断）

## 自算核对（源文章数字重算结果）

- fixed_str 实测长度 64，且 64 个字符互不重复 → 满足 %64 取到合法下标
- md5(32 hex)+\u0000 = 33 字符 → 33/3 = 11 组 → 11*4 = 44 → 与常见 x-zse-96 44 长度一致（文章未给长度，属自算核对）
- 1651578807 换算 UTC 2022-05-03 11:53:27，文章发布 2022-05-04 → 量级为 unix 秒
- m1(1,2,3) 实算 = 197163（用于确认位运算算式）

## 明确留白（原文未给出，不要臆测）

- 文章从未出现 x-zse-93 / d_c0 字样；'101_3_2.0 是 x-zse-93'、'APAdT...= 是 d_c0' 均为我推断，未定位证据
- 4 个位运算函数 m1..m4 与 11 个分组如何对应（哪组用哪个）原文未说明
- sign() 内是否还有其它常量/盐值/环境指纹，原文未给
- source 里的 + 与 | 是字面分隔符还是字符串拼接运算符，原文未明说（| 亦可能是疑似 cookie 自带的 值|时间戳 结构）
- 输出是否带 2.0_ 前缀：本文章完全未提及
- x-zse-96 放 header 的逐字大小写未给出
- 正文无可运行代码，仅『成功留念』截图 + github 源链接，证据强度属作者自述成功
