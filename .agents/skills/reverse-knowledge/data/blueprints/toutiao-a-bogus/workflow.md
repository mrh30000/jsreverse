## 入口与传输

- 生成位置: bdms 文件（作者：『发现最终的加密逻辑都是在这个bdms文件里面』）；入口方法 s，核心加密逻辑是其中的 U 方法
- 触发时机: 请求前（feed 请求加载当前页面内容时）
- 传输方式: 载荷(query)参数，参数名文章逐字写作 `a-bogus`（连字符）

## 排查步骤

1. 搜关键字搜不到，改用 XHR 断点断在 feed 请求上（可断住）。
2. 跟栈进入 bdms 文件，下日志断点确认此处输出 a-bogus，即入口。
3. 下条件断点（长度==168）确认断点命中；注意 a-bogus 长度不固定，作者随后改成『长度大于168』。
4. 进入 s 方法，核心加密逻辑是 U 方法；把 U 方法导出并输出其 return。
5. 全扣：把 bdms 整体扣下来（约一万行代码）单独放一个文件。
6. 挂代理补环境（一直补），补到出值为止，输出长度 160。
7. 发请求验证，响应 200，成功拿到数据。

## 算法口径

- 家族: `unknown`
- 细节: 文章未给出算法细节（全篇为截图 + 文字步骤），仅指出最终加密逻辑在 bdms 文件里、核心是 U 方法
- 输出编码: unknown
- 输出长度: 文章明确说『a-bogus的长度不是固定的』；作者观察值 168，补环境后输出 160

## 自算核对（源文章数字重算结果）

- 该篇 61 行中，第 28 行是 16414 字符的内嵌 base64 图片（data:image/png;base64,...），正文仅有步骤文字 + 截图，属 screenshot-only 级别证据。
- 行数核对：全文 61 行 / 19360 字节，其中图片行 13,17,21,25,28,31,35,37,41,43,47,51,55,59 共 14 行是图；正文步骤只有 11 条编号文字。
- 文章未出现任何算法名（SM3/RC4/base64）、字母表、密钥、盐值、参数拼接顺序——因此本蓝图 algorithm 除 family=unknown 外全部留空。

## 来源之间的矛盾

- 52pojie-2048032-...:15 vs 52pojie-2048032-...:27 vs 52pojie-2048032-...:53 —— 长度 168 vs 160 vs 『不是固定的』（判定: 作者自述长度不固定，168/160 为不同环境观测值；非硬矛盾，但『固定长度做条件断点』的写法本身被作者自己否定了）

## 明确留白（原文未给出，不要臆测）

- 参数名到底是 a-bogus（连字符）还是 a_bogus（下划线）：文章正文一律写 a-bogus，但无代码字面量佐证（screenshot-only），故标 unknown。
- s 方法 / U 方法的入参、内部算法、输出编码全部未知。
- 是否与抖音 a_bogus 同源（都用 bdms）无法从本篇判定，需要另外的字面量证据。
- 168 / 160 之外的其它长度分布未知。
